# Plan: /brainstorm Skill + Architecture-Map Discoverability + VS Code Mermaid

- **Date**: 2026-05-03
- **Status**: Complete (shipped — `/brainstorm` skill + `scripts/brainstorm-round.mjs` + 7 modules under `scripts/lib/brainstorm/` (arch-context, debate-prompt, depth-config, file-lock, gemini-adapter, id-validator, insight-store); `--with-arch` flag wired; `docs/architecture-map.md` generated by `npm run arch:render`. Status line corrected 2026-05-23 from "Draft v6 (post R1+R2+R3 GPT + Gemini ×2)".)
- **Author**: Claude + Louis

## 1. Context Summary

Two small features bundled because they ship together and touch the same set
of repos:

1. **`/brainstorm` skill** — user wants a thinking partner that loops in 1–2
   external LLMs (OpenAI default, Gemini opt-in) so they can compare
   independent perspectives. Convergence is **manual** — Claude does not
   auto-synthesise, the user drives when to converge.

2. **Architecture-map discoverability + Mermaid viewing** — each consumer repo
   has `docs/architecture-map.md` (generated by the architectural-memory
   pipeline) but it isn't linked from anywhere obvious, and VS Code's built-in
   Markdown preview doesn't render Mermaid without an extension.

What exists today:

- **Skills already in `c:/GIT/claude-audit-loop/skills/`**: plan, plan-backend,
  plan-frontend, audit-loop (shim), audit-plan, audit-code, ship, ux-lock,
  persona-test, cycle, explain. No brainstorm yet.
- **Existing helper-script pattern** in this repo (`scripts/openai-audit.mjs`,
  `scripts/gemini-review.mjs`): ESM CLI, takes flags, calls SDK, normalizes
  through Zod schemas, returns `{result, usage, latencyMs}`, logs progress to
  stderr, JSON to stdout — this is the pattern the brainstorm helper must
  match (R1-H1).
- **Model resolver** at `scripts/lib/model-resolver.mjs` — `resolveModel(id)`
  takes a sentinel (`latest-gpt`, `latest-pro`, etc.) or concrete ID and
  returns a current concrete ID. All new code MUST go through it (R1-H4).
- **Secret patterns** at `scripts/lib/secret-patterns.mjs` — `scanForSecrets()`
  + `redactSecrets()`. Used for the same defense-in-depth purpose
  brainstorm needs for outbound topic text (R1-H5).
- **`docs/architecture-map.md`** is committed in all 3 repos but not linked
  from README/AGENTS in any of them.
- **Mermaid rendering**: works natively in GitHub PRs, Obsidian, Claude Code.
  Only VS Code's built-in MD preview needs `bierner.markdown-mermaid`.
- **`scripts/sync-to-repos.mjs`** maintains `CORE_SCRIPTS`, `ARCH_MEMORY_SCRIPTS`,
  `CLAUDE_CODE_FILES` constants and propagates skill bundle to consumers.
- **`.vscode/`** state per repo:
  - audit-loop: `mcp.json` only (not gitignored)
  - wine-cellar: `mcp.json`, `settings.json`, `tasks.json` — only `mcp.json`
    gitignored (line 120)
  - ai-organiser: `mcp.json`, `settings.json` — entire `.vscode/` gitignored
    (line 6, then `mcp.json` again at line 85)

## 2. Proposed Architecture

### 2.1 `/brainstorm` Skill — Two-Layer Design (R1-H1, R1-H2)

**SKILL.md is a thin orchestration shim.** All API calls go through a Node
helper inside the repo. This matches the rest of the codebase
(`scripts/openai-audit.mjs`, `scripts/gemini-review.mjs`) and gets us:
typed responses, sentinel resolution, cost tracking, secret redaction,
testability — none of which a curl-in-shell version can give us cleanly.

**Delivery scope (R2-H1)**: v1 brainstorm runs **only when invoked from a
repo that has the synced helper bundle** (audit-loop, wine-cellar,
ai-organiser — anything that consumes `sync-to-repos.mjs`). The helper is
not standalone-installable. There is **no user-level mirror** in v1 — that
would require the helper to be on PATH or installed to a fixed home location,
which is a separate delivery problem deferred to v2 (§8).

**File layout**:

```
skills/brainstorm/SKILL.md                 # Thin orchestrator (this is what Claude reads)
scripts/brainstorm-round.mjs               # CLI entry point — takes flags + topic, emits JSON
scripts/lib/brainstorm/openai-adapter.mjs  # OpenAI provider — uses installed openai SDK
scripts/lib/brainstorm/gemini-adapter.mjs  # Gemini provider — uses @google/genai SDK
scripts/lib/brainstorm/schemas.mjs         # Zod schemas for normalized response shape
scripts/lib/brainstorm/prompt.mjs          # Single source of truth for the concept-level system prompt (R2-L1)
tests/brainstorm-round.test.mjs            # Node test runner — unit tests for adapters + redaction
```

**SKILL.md contract** (short):
1. Parse `$ARGUMENTS` → flags + topic.
2. Print "Calling providers (sentinels: openai=`latest-gpt`, gemini=`latest-pro`)…"
   — show the **sentinels requested**, not resolved IDs (Gemini-G2). Resolution
   happens inside the helper; the kickoff card cannot know the concrete IDs
   yet without an extra round-trip. The post-call rendering (Step 5) shows
   the resolved IDs from the helper's `resolvedModels` field.
3. Pass the topic via a **temp file written by Claude's `Write` tool**, then
   pipe to the helper's stdin. This bypasses Bash heredoc semantics entirely —
   no expansion risk and no delimiter-collision risk (Gemini-G1 v1+v2):
   ```
   # Claude writes the topic via the Write tool to:
   #   <repo>/.claude/tmp/brainstorm-<sid>.txt
   # Then runs:
   node scripts/brainstorm-round.mjs --topic-stdin --models openai[,gemini] [--openai-model latest-gpt] [--gemini-model latest-pro] < .claude/tmp/brainstorm-<sid>.txt
   # And cleans up:
   rm -f .claude/tmp/brainstorm-<sid>.txt
   ```
   `<sid>` is the brainstorm session ID (epoch ms or random hex). The
   temp dir `.claude/tmp/` lives **inside the repo** (gitignored — see
   §4) so file permissions are governed by the user's existing repo
   ACLs, not the world-readable `/tmp` umask (Gemini-G3). The temp file
   is removed after the helper returns.

   Heredoc is **not** used — the v5 design with `<< 'EOF_BRAINSTORM_TOPIC'`
   was rejected after Gemini's round-2 finding that any heredoc delimiter
   (random or fixed) is fragile to topic content. Write-then-pipe via a
   real file is the simpler safe path.

   Argv `--topic` remains for ad-hoc CLI use by humans, but SKILL.md always
   uses temp-file + `--topic-stdin`.
4. Read JSON stdout → render per-provider blocks per the state table (§2.4),
   and include the resolved concrete model IDs from `resolvedModels` (e.g.
   `### OpenAI (gpt-5.4)` not `### OpenAI (latest-gpt)`) — Gemini-G2.
5. Add Claude's own take.
6. Stop. Wait for user.

The concept-level system prompt is **defined only in
`scripts/lib/brainstorm/prompt.mjs`** (R2-L1). SKILL.md describes its
*intent* in human terms but does not reproduce the wording — single source
of truth. AC4 (§9a) greps the prompt module, not SKILL.md.

**`scripts/brainstorm-round.mjs` contract**:

Input flags:
| Flag | Default | Meaning |
|---|---|---|
| `--topic <text>` | required | User's topic (passed via argv, not via shell-interpolated JSON — R1-H2) |
| `--topic-stdin` | off | Alternative: read topic from stdin (for topics with awkward shell quoting) |
| `--models <csv>` | `openai` | `openai`, `gemini`, or both |
| `--openai-model <id>` | `latest-gpt` | Sentinel or concrete ID — resolved via `resolveModel()` (R1-H4) |
| `--gemini-model <id>` | `latest-pro` | Same |
| `--max-tokens <n>` | `1500` | Per-provider response cap (drives cost ceiling — R1-L10) |
| `--out <path>` | stdout | JSON output destination |

**No `--no-redact` flag (R2-H3)** — redaction is mandatory in v1. False
positives are handled by the user reviewing the rendered topic (with
`[redacted-N]` placeholders) and deciding whether to retry with a
hand-cleaned `--topic` they explicitly trust. A per-category opt-out is a
v2 concern (§8).

**Output contract is total (R2-H4)**: the helper **always emits one
schema-valid JSON document** to stdout (or `--out`), regardless of provider
outcomes. Exit codes:

| Exit | When |
|---|---|
| `0` | Helper ran. JSON document is on stdout/`--out`. Each provider has its own `state` field — caller reads those, not the exit code, to know which providers worked. |
| `1` | Argv error (bad flag, missing required), OR the helper itself produced a schema-invalid document (bug). Stderr has a one-line `code: <SLUG>` reason. |

This means SKILL.md can render deterministic provider blocks even if every
provider failed, including the misconfigured case below.

JSON output schema (Zod, in `scripts/lib/brainstorm/schemas.mjs`):

```typescript
{
  topic: string,                          // post-redaction (with `[redacted-N]` placeholders if any)
  redactionCount: number,                 // how many secret patterns were redacted
  resolvedModels: { openai?: string, gemini?: string },  // sentinel → concrete IDs
  providers: Array<{          // Order MUST match the order requested in --models (R3-M2). Out-of-order completion does not affect emit order.
    provider: 'openai' | 'gemini',
    state: 'success' | 'misconfigured' | 'timeout' | 'http_error' | 'empty' | 'malformed' | 'blocked',
    text: string | null,                  // null on non-success
    errorMessage: string | null,          // populated on non-success
    httpStatus: number | null,            // populated on http_error
    usage: { inputTokens: number, outputTokens: number } | null,
    latencyMs: number,                    // 0 for misconfigured (no call attempted)
    estimatedCostUsd: number | null
  }>,
  totalCostUsd: number
}
```

`misconfigured` (R2-H2) is set when a requested provider can't be called at
all — typically a missing API key. The helper does **not** exit 1; it skips
the call, populates `state: 'misconfigured'` with `errorMessage: 'OPENAI_API_KEY
not set'` (or similar), and continues with the other provider(s). If every
requested provider is misconfigured, exit is still 0 — the JSON document
makes the failure visible.

**Provider adapters** (one per provider, identical interface):

```typescript
async function call({ topic, model, maxTokens, signal }) → ProviderResult
```

Each adapter:
- Uses the installed SDK (`openai` 6.17, `@google/genai` 1.47 — already in `package.json`)
- Wraps the call in a 60s `AbortController` timeout
- Catches and classifies errors into the state enum above
- Returns the normalized shape — never throws to the caller

**Concept-level prompt** (single source: `scripts/lib/brainstorm/prompt.mjs`,
sent to both providers verbatim — R2-L1):
> You are a thoughtful brainstorming partner. The user is exploring an idea
> and wants your independent perspective alongside other AI models'.
> - Push back where you disagree. Don't be deferential.
> - Surface trade-offs, hidden assumptions, second-order effects.
> - Propose 1–2 concrete alternatives if you see a different path.
> - Be opinionated. 250–500 words.

(The wording above is illustrative in this plan; the canonical wording lives
in `prompt.mjs`. AC4 grep targets that file.)

**Topic discipline** (R1-H5):
- Only the user-supplied `--topic` text is sent. **No** auto-injection of
  conversation summary, file contents, repo context, AGENTS.md excerpts, or
  recent transcript. The previous draft floated "Conversation so far: …"
  auto-injection — **dropped** in v2.
- Pre-send: topic runs through `redactSecrets()` from `scripts/lib/secret-patterns.mjs`.
  Redaction is **mandatory** — there is no opt-out flag (R2-H3). If anything
  is redacted, kickoff card shows `⚠ Redacted N secret(s) before sending`
  and the JSON output exposes `redactionCount` so the user can see it
  happened. False positives are handled by the user reviewing the rendered
  topic and choosing to retry with a hand-cleaned `--topic`.
- v2 may add `--include-file <path>` for explicit file inclusion. Not in v1.

**Pre-flight checks (R2-H2)**:
- Pre-flight does **not** exit on missing API keys. Instead, each missing
  key is recorded as a `misconfigured` provider state in the JSON output;
  remaining providers run normally.
- Resolved model IDs printed in kickoff card so the user sees what was
  actually used (post-sentinel resolution).
- Argv errors (bad flag, missing required `--topic`/`--topic-stdin`) DO
  exit 1 — those are programmer errors, not runtime states.

**Malformed-payload retention (R2-M3, Gemini-G3)**:
- When a provider returns a `malformed` response, the raw payload is saved
  for debugging. Path is `path.join(repoRoot, '.claude/tmp/brainstorm-<sid>-<provider>.json')`
  — **inside the repo, not `os.tmpdir()`**. This puts file permissions
  under the user's repo ACLs instead of the world-readable `/tmp` umask
  (Gemini-G3) and guarantees Windows compatibility.
- The saved payload is run through `redactSecrets()` first — same defense
  in depth as the outbound topic.
- File written with `fs.writeFileSync(path, data, { mode: 0o600 })` for
  defense in depth even inside the repo.
- `.claude/tmp/` is added to `.gitignore` in audit-loop and synced to
  consumers (see §4 — file-level plan). The directory is created on demand
  by the helper.
- Retention: cleaned automatically by the SKILL.md (`rm -f .claude/tmp/brainstorm-<sid>.txt`
  after each call, before the synthesis prompt). Stale debug JSONs are
  user-deletable; we do not write a janitor.

### 2.2 Architecture-Map Discoverability (R1-M6, R1-L9)

Add a one-line pointer in **two places per repo**, identical wording across
repos for consistency. Use **HTML comment marker pairs** as anchors so the
update is idempotent and a future regenerator can find + replace the block
deterministically.

**README.md** — anywhere appropriate (typically near top, before "Setup"):
```markdown
<!-- arch-map-discoverability:start -->
## Architecture

See [`docs/architecture-map.md`](docs/architecture-map.md) — a generated
index of every symbol in this repo, grouped by domain, with Mermaid
diagrams. Regenerated on `/ship` or via `npm run arch:render`.
<!-- arch-map-discoverability:end -->
```

**AGENTS.md** — anywhere in the project overview:
```markdown
<!-- arch-map-discoverability:start -->
> **Architecture map**: `docs/architecture-map.md` is the live, generated
> index of every symbol in this repo. Start there when you need to find an
> existing function, class, or component before writing a new one.
<!-- arch-map-discoverability:end -->
```

**Updater behaviour** (manual via `Edit` tool for v1; if scripted later it
must be a tested updater per below):

For each target file:
1. `Grep` for the start marker `<!-- arch-map-discoverability:start -->`.
2. If present → **skip** (idempotent — no second insertion).
3. If absent → `Edit` to insert the block. Preferred insertion point per file
   type:
   - README.md: after the H1 (first `# ` line) and any badges, before any
     existing H2.
   - AGENTS.md: immediately after the H1 + the `> **Canonical project context...**`
     blockquote if present, else after the H1.
4. **No fuzzy fallback** — if the preferred anchor is genuinely missing
   (no H1), report that and stop. Don't improvise placement.

If we ever script this (out of scope for v1 — see §8), the script must
assert exactly one `<!-- arch-map-discoverability:start -->` per target after
running, and exit non-zero otherwise.

### 2.3 VS Code Mermaid Recommendation (R1-H3)

Per repo, add `.vscode/extensions.json`:
```json
{
  "recommendations": [
    "bierner.markdown-mermaid"
  ]
}
```

**ai-organiser .gitignore fix** (R1-H3 — critical):

Git's `.gitignore` semantics: **a negation `!file` does NOT re-include a file
whose parent directory is excluded.** ai-organiser's current `.gitignore` has
`.vscode/` (line 6) — that excludes the directory itself, so
`!.vscode/extensions.json` would silently fail.

Fix: change line 6 from `.vscode/` to `.vscode/*` (ignore *contents*, not
directory). Then `!.vscode/extensions.json` works. Apply this exact change:

```diff
-.vscode/
+.vscode/*
+!.vscode/extensions.json
```

Also remove the duplicate `.vscode/mcp.json` at line 85 since it's redundant
under `.vscode/*`.

**Verification step** (added to acceptance, R1-H3):
```bash
# In ai-organiser, after editing .gitignore:
git check-ignore -v .vscode/extensions.json
# Expected exit code: 1 (file is NOT ignored — meaning the negation works)
git check-ignore -v .vscode/mcp.json
# Expected exit code: 0 (file IS ignored — meaning .vscode/* still applies)
```

Both checks must pass before staging the file.

**Per-repo handling**:

| Repo | `.vscode/` gitignore state | Action |
|---|---|---|
| audit-loop | not gitignored | Create `.vscode/extensions.json` (commit) |
| wine-cellar | `.vscode/mcp.json` only ignored | Create `.vscode/extensions.json` (commit) |
| ai-organiser | entire `.vscode/` ignored | Apply `.vscode/*` + negation per above; verify with `git check-ignore`; create file |

**Merge-safe**: if `.vscode/extensions.json` already exists (e.g. workspace
already recommends other extensions), **read first**; parse the JSON; add
`bierner.markdown-mermaid` to `recommendations` only if not present;
otherwise no-op.

### 2.4 Provider-State Rendering Table (R1-M8, R2-M1)

The helper is synchronous — it returns one JSON document after all
providers settle. There is no `loading` state in the data contract (R2-M1);
SKILL.md prints a single top-level "Calling providers…" message before
invoking the helper, and the per-provider blocks appear after the helper
returns.

Each provider entry in the JSON renders one block in SKILL.md output.
State drives the block's contents:

| State | Block contents | When |
|---|---|---|
| `success` | `### <provider> (<model>)`<br>`<text verbatim>` | HTTP 200, parsed text non-empty |
| `misconfigured` | `### <provider>`<br>`⚠ Not called: <errorMessage>` (e.g. "OPENAI_API_KEY not set") | Required env var missing — provider was skipped entirely (R2-H2) |
| `timeout` | `### <provider> (<model>)`<br>`⚠ Timeout after 60s. Try again or use --max-tokens=...` | AbortController fired |
| `http_error` | `### <provider> (<model>)`<br>`⚠ HTTP <status>: <provider error message verbatim>` | Non-2xx response |
| `empty` | `### <provider> (<model>)`<br>`⚠ Empty response (provider returned no content). Possible causes: blocked by safety filter, max_tokens=0.` | HTTP 200 but no text |
| `malformed` | `### <provider> (<model>)`<br>`⚠ Malformed response — could not parse <field>. Raw payload saved to <os-tmpdir>/brainstorm-<sid>-<provider>.json.` | Schema validation failed |
| `blocked` | `### <provider> (<model>)`<br>`⚠ Blocked by safety filter: <category>` | Gemini `PROHIBITED_CONTENT` etc. |

**Partial success**: if one provider succeeds and one fails, both blocks
still render — the failure does not prevent the success from being shown.
Claude's own take always renders last regardless.

## 3. Sustainability Notes

- **Brainstorm skill model IDs auto-roll** via sentinels (`latest-gpt`,
  `latest-pro`). Quarterly STATIC_POOL refresh in the resolver covers drift
  for both. Override flags exist as escape hatches.
- **Discoverability link is durable**. `docs/architecture-map.md` path is
  baked into the architectural-memory pipeline (`scripts/symbol-index/render-mermaid.mjs`).
  HTML comment markers make future regeneration straightforward.
- **VS Code extension recommendation is non-binding**. VS Code prompts but
  doesn't force install. If the extension is renamed or replaced, update one
  line in 3 files.
- **Adapter swap-in/out**: provider adapters share an interface. Adding a
  third provider (e.g. Anthropic Haiku) is a new file under `scripts/lib/brainstorm/`,
  no orchestrator changes.
- **Brainstorm skill won't drift via sync**. SKILL.md is static markdown;
  `brainstorm-round.mjs` + adapters get added to `CORE_SCRIPTS` so they
  sync with the rest of the bundle.

## 4. File-Level Plan

### A. Brainstorm — code

| File | Status | Purpose |
|---|---|---|
| `skills/brainstorm/SKILL.md` (audit-loop) | NEW | Thin orchestrator — invokes the helper |
| `scripts/brainstorm-round.mjs` | NEW | CLI entry — flag parsing, dispatch to adapters, JSON output |
| `scripts/lib/brainstorm/openai-adapter.mjs` | NEW | OpenAI SDK call + error classification |
| `scripts/lib/brainstorm/gemini-adapter.mjs` | NEW | Gemini SDK call + error classification |
| `scripts/lib/brainstorm/schemas.mjs` | NEW | Zod schemas for normalized response |
| `scripts/lib/brainstorm/prompt.mjs` | NEW | Single source of truth for the concept-level system prompt (R2-L1) |
| `tests/brainstorm-round.test.mjs` | NEW | Adapter unit tests + redaction tests |
| `scripts/sync-to-repos.mjs` | EDIT | Add `brainstorm-round.mjs` + `scripts/lib/brainstorm/*` files to `CORE_SCRIPTS`; add `brainstorm` to skill set; add `.claude/tmp/` to consumer-side `.gitignore` template |
| `.gitignore` (audit-loop) | EDIT | Add `.claude/tmp/` (brainstorm temp topic files + malformed payload debug JSONs — Gemini-G3) |

**Notes**:
- `.claude/skills/brainstorm/SKILL.md` is **generated** from
  `skills/brainstorm/SKILL.md` by `npm run skills:regenerate` — do not
  hand-edit the generated copy.
- **No `~/.claude/skills/brainstorm/SKILL.md` mirror in v1** (R2-H1). The
  helper isn't reachable from a non-repo cwd. v1 brainstorm runs only in
  the 3 repos that consume the sync. Standalone-installable v2 is a separate
  delivery story (§8).

### B. Discoverability changes

| File | Status | Edit |
|---|---|---|
| `c:/GIT/claude-audit-loop/README.md` | EDIT | Add anchored block (skip if marker present) |
| `c:/GIT/claude-audit-loop/AGENTS.md` | EDIT | Same |
| `c:/GIT/wine-cellar-app/README.md` | EDIT | Same |
| `c:/GIT/wine-cellar-app/AGENTS.md` | EDIT | Same |
| `c:/GIT/ai-organiser/README.md` | EDIT | Same |
| `c:/GIT/ai-organiser/AGENTS.md` | EDIT | Same |

### C. VS Code mermaid

| File | Status | Edit |
|---|---|---|
| `c:/GIT/claude-audit-loop/.vscode/extensions.json` | NEW or MERGE | `bierner.markdown-mermaid` |
| `c:/GIT/wine-cellar-app/.vscode/extensions.json` | NEW or MERGE | Same |
| `c:/GIT/ai-organiser/.vscode/extensions.json` | NEW or MERGE | Same |
| `c:/GIT/ai-organiser/.gitignore` | EDIT | `.vscode/` → `.vscode/*` + `!.vscode/extensions.json`; remove duplicate `.vscode/mcp.json` |

## 5. Risk & Trade-off Register

| Risk | Mitigation |
|---|---|
| OpenAI/Gemini model IDs go stale | Sentinels via `resolveModel()`; user can override per-call |
| Curl call hangs | `AbortController` 60s timeout in adapters; rendered as `timeout` state |
| User invokes `/brainstorm` outside any project (no `.env`) | Adapter reads env vars only; pre-flight surfaces missing key by name |
| Discoverability link added twice on re-run | HTML comment marker idempotence |
| Existing `.vscode/extensions.json` overwritten | Read-first merge logic |
| ai-organiser gitignore negation silently fails | Use `.vscode/*` (contents) instead of `.vscode/` (dir); verify with `git check-ignore` |
| Brainstorm responses leak sensitive context | `redactSecrets()` on topic before send; `redactionCount` surfaced; topic-only (no auto file injection) |
| Cost surprise | Helper computes a **pre-call estimate** = `(input_chars/4 × input_price + max_tokens × output_price) × providers` per resolved model, prints to stderr before the API calls, and includes it in the kickoff message. Post-call `totalCostUsd` field reflects actual usage. The previous v5 design (`<max_tokens × 2> ≤ $0.05` static formula) was rejected after Gemini-G2 v2 — it ignored input cost and would mislead by 10–100× on large topic-paste inputs. |
| Synthesis happens too early (Claude jumps in) | Hard rule in SKILL.md: synthesis only on explicit user signal; trigger phrases listed |
| Adapter throws unhandled | Adapters MUST never throw to caller — catch + classify into state enum; `brainstorm-round.mjs` exits non-zero only on argv errors |
| **Shell injection via topic text containing `$(…)`, backticks, or any heredoc delimiter (Gemini-G1 v1 + v2)** | **SKILL.md MUST use the temp-file pattern** — write topic via Claude's `Write` tool to `.claude/tmp/brainstorm-<sid>.txt`, then pipe `< file` to the helper. No heredoc, no shell-string interpolation. AC24 verifies the temp-file pattern is present and forbidden patterns (heredoc, `printf '%s' "$TOPIC"`) are absent. |
| **Kickoff card hallucinates resolved model IDs (Gemini-G2)** | Pre-call kickoff card shows the **sentinel requested** (`latest-gpt`), not the resolved concrete ID. Post-call rendering uses `resolvedModels` from the JSON output. Trying to resolve in SKILL.md without invoking the helper would either guess or require a separate `--resolve-only` round-trip — both deferred to v2 (§8). |

## 6. Testing Strategy

**Unit tests** (`tests/brainstorm-round.test.mjs`, run via `npm test`):
- Argv parsing — `--models openai`, `--models openai,gemini`, `--models gemini`, missing topic, both `--topic` and `--topic-stdin` simultaneously (error)
- Sentinel resolution — `latest-gpt` resolves to a concrete ID; concrete ID passes through
- Schema validation — valid response passes; missing fields fail loudly
- Secret redaction — topic with fake `sk-...` is redacted before adapter receives it; `redactionCount` correct
- Adapter error classification — mock SDK rejection with HTTP 401 → `http_error`; mock timeout → `timeout`; mock empty content → `empty`
- **Missing key (R2-H2, R3-H2)** — `OPENAI_API_KEY` unset, `--models openai`: helper exits **0**; output JSON has `providers[0].state == 'misconfigured'` and `errorMessage` names the variable
- **All-providers-failed (R2-H4, R3-H2)** — both adapters mocked to fail (one timeout, one http_error): helper exits **0**; output JSON is schema-valid; both states present
- **Provider order (R3-M2)** — `--models gemini,openai` with both succeeding: output `providers[0].provider == 'gemini' && providers[1].provider == 'openai'`, regardless of which finishes first
- **Stdin topic** — `printf 'multi-line\ntopic' | node scripts/brainstorm-round.mjs --topic-stdin --models openai`: topic in JSON output preserves newline

**Integration smoke** (manual, requires real API keys):
1. `printf 'what is the right backup strategy for a 50GB SQLite db?' | node scripts/brainstorm-round.mjs --topic-stdin --out /tmp/bs1.json` → exit 0, JSON has 1 provider entry, `state: success`
2. Same with `--models openai,gemini` → 2 entries, both `success`
3. `OPENAI_API_KEY= printf test | node scripts/brainstorm-round.mjs --topic-stdin` → **exit 0** (R2-H2, R3-H2), JSON has `providers[0].state == 'misconfigured'`
4. `printf 'my key is sk-12345abcdef67890ghijk' | node scripts/brainstorm-round.mjs --topic-stdin --models openai` → JSON shows `redactionCount: 1`, topic in payload contains `[redacted-openai-key]`

**Discoverability** (visual):
- Open README.md in each repo on GitHub — link is clickable, lands on
  rendered architecture-map.md
- Re-run insertion against an already-edited file — no duplicate (Grep finds
  marker, Edit skipped)

**VS Code mermaid** (manual):
- ai-organiser: after `.gitignore` edit, `git check-ignore -v .vscode/extensions.json` → exit 1
- ai-organiser: `git check-ignore -v .vscode/mcp.json` → exit 0 (still ignored)
- All 3 repos: open in fresh VS Code window — extension prompt fires; install
  it; open `docs/architecture-map.md` preview — Mermaid renders

## 7. Rollout

**Pre-implementation: architectural-memory consultation (R2-M2).** Per the
AGENTS.md "Pre-fix Consultation" rule, run `get-neighbourhood` for the new
symbols before writing code:

```bash
node scripts/cross-skill.mjs get-neighbourhood --json '{
  "targetPaths": ["scripts/brainstorm-round.mjs",
                  "scripts/lib/brainstorm/openai-adapter.mjs",
                  "scripts/lib/brainstorm/gemini-adapter.mjs",
                  "scripts/lib/brainstorm/schemas.mjs",
                  "scripts/lib/brainstorm/prompt.mjs"],
  "intentDescription": "CLI helper that calls OpenAI + Gemini concurrently with concept-level prompt, returns normalized Zod-validated JSON with per-provider state",
  "k": 8
}'
```

Record the recommendation column for each candidate near-duplicate in a
`### Neighbourhood considered` block in this plan before starting code.
Recommendations expected: **reuse** the existing `resolveModel()`,
`scanForSecrets()`/`redactSecrets()`, and the `{result, usage, latencyMs}`
shape from `openai-audit.mjs` / `gemini-review.mjs`. **Justify-divergence**
is expected if a brand-new prompt module is introduced (it intentionally
diverges from the audit prompts because brainstorm is concept-level, not
audit-style).

### Neighbourhood considered (consulted 2026-05-03)

Top 8 candidates returned by `cross-skill.mjs get-neighbourhood` for the
new brainstorm files. All returned `recommendation: review` (similarity
0.71–0.74) — no `reuse`/`extend`-tier matches because the brainstorm
helper is genuinely a new shape (multi-provider concurrent calls + total
output contract + concept-level prompt). Decisions:

| Candidate | Sim | Recommendation | Decision |
|---|---|---|---|
| `scripts/gemini-review.mjs:callGemini` (L278) | 0.74 | review | **justify-divergence** — single-call Zod-validated review wrapper; brainstorm's per-provider state enum + parallel-call shape is sufficiently different. |
| `scripts/lib/llm-wrappers.mjs:callGemini` (L53) | 0.74 | review | **justify-divergence** — same as above; single-call wrapper. |
| `scripts/audit-full.mjs:main` (L44) | 0.74 | review | Different layer (orchestrator); not a peer to brainstorm-round.mjs. |
| `scripts/openai-audit.mjs:main` (L1840) | 0.72 | review | Different layer (multi-pass audit orchestrator). |
| `scripts/gemini-review.mjs:callClaudeOpus` (L386) | 0.72 | review | Different provider (Anthropic fallback); reuse via shared adapter interface only. |
| `scripts/lib/model-resolver.mjs:_cli` (L447) | 0.71 | review | CLI plumbing for resolver self-test, not a model caller. |
| `scripts/symbol-index/embed.mjs:main` (L76) | 0.71 | review | Different domain (embedding pipeline). |
| `scripts/gemini-review.mjs:runPingGemini` (L656) | 0.71 | review | Connectivity smoke test, not a generation caller. |

**Mandatory reuse** (independent of similarity scoring — these are project-wide rules):
- `resolveModel()` from `scripts/lib/model-resolver.mjs` (R1-H4 — sentinel resolution)
- `redactSecrets()` from `scripts/lib/secret-patterns.mjs` (R1-H5 / Gemini-G3 — outbound topic + saved payloads)
- `{result, usage, latencyMs}` return contract pattern (matches `openai-audit.mjs` + `gemini-review.mjs`)
- Pricing-table approach from `openai-audit.mjs` `modelPricing` (per-model `{ input, output }` per-1M rates) — **mirror locally** in `scripts/lib/brainstorm/pricing.mjs` rather than centralising (R3-M1 deferral rationale: only 2 callers).

Then sequential, repo-by-repo:

1. **audit-loop** (source of truth):
   - Write Node helper + adapters + schemas + prompt + tests
   - Write `skills/brainstorm/SKILL.md`
   - Run `npm test` — all brainstorm tests green
   - Add `brainstorm-round.mjs` + `scripts/lib/brainstorm/*` to `CORE_SCRIPTS` in `sync-to-repos.mjs`
   - Add `brainstorm` to the synced skill set in `sync-to-repos.mjs`
   - Add `.vscode/extensions.json`
   - Edit `README.md` + `AGENTS.md` (idempotent — check marker first)
   - Run `npm run skills:regenerate`
   - **Run `npm run sync` (R3-H4)** — pushes the new helper bundle to consumer
     repos before they install. Without this, step 2's `skills:install` would
     pick up the SKILL.md but find no helper script.
   - Commit + push
2. **wine-cellar**:
   - `git pull` (gets the synced helper files into the worktree)
   - Run `npm run skills:install` (registers the SKILL into `.claude/skills/`)
   - Verify helper actually arrived (R3-H4): `test -f scripts/brainstorm-round.mjs && test -f scripts/lib/brainstorm/openai-adapter.mjs && node scripts/brainstorm-round.mjs --help`
   - Add `.vscode/extensions.json`, edit `README.md` + `AGENTS.md`
   - Commit + push
3. **ai-organiser**: same as wine-cellar, plus `.gitignore` patch + verify
   with `git check-ignore`.

## 8. Out of Scope

- **Standalone-installable brainstorm** (R2-H1) — v1 is repo-bound. To make
  brainstorm work from any cwd we'd need to either (a) publish the helper
  as an npm CLI on PATH, or (b) ship an installer that copies helper +
  node_modules to a fixed home directory. Both add packaging surface
  outside the scope of this plan. v2 task.
- **Brainstorm session memory** — not persisting brainstorm transcripts to
  Supabase. Convergence is conversational and ephemeral; durable state
  belongs in the user's hand-written follow-up plan.
- **More than 2 LLMs per round** — Claude + 2 external is already 3
  perspectives, the diminishing-returns point per audit-triad findings.
- **Cost preflight gate** — brainstorm cost is bounded by `max_tokens × 2`
  per provider, so a hard ceiling is sufficient; the audit-style preflight
  prompt is overkill for sub-$0.05 calls.
- **Per-category redaction opt-out (R2-H3)** — `--no-redact` was rejected as
  a footgun. A safer opt-out (e.g. `--allow-redaction-category aws`) with
  an explicit confirmation step is a v2 concern; v1 has no opt-out.
- **Custom system prompts per call** — single concept-level prompt covers
  the goal. v2 may add `--style <preset>`.
- **Auto file/context injection** — explicitly dropped in v2 (R1-H5). No
  auto-summary of conversation. v2 may add `--include-file <path>` with
  explicit opt-in + redaction.
- **Scripted discoverability updater** — v1 is manual via `Edit` tool with
  marker idempotence. A scripted version (with assert-one-marker post-check)
  is a future maintenance task.
- **Streaming/per-provider loading state (R2-M1)** — would require streaming
  helper output (e.g. NDJSON-per-event) and SKILL.md consuming it
  incrementally. v1 helper is synchronous; UX is "Calling providers…" then
  the assembled blocks appear together. Worth revisiting if a single
  provider call regularly exceeds 30s.
- **Structured per-provider diagnostics (R3-H3)** — extend the schema with
  `diagnosticId`, `debugArtifactPath`, `blockedCategory`, etc. so SKILL.md
  renders from typed fields instead of free-text `errorMessage`. v1
  uses free text; the rendering table in §2.4 is good enough for human
  consumption. Revisit when machine consumers (dashboards) start parsing
  the JSON.
- **Centralized model pricing module (R3-M1)** — v1 helper estimates cost
  inline using a small map of resolved model IDs → per-token prices.
  Centralising into a shared pricing module (with explicit unknown-price
  handling, rounding, and tests) is the right end state but only worth it
  once 3+ scripts need pricing. Today only `openai-audit.mjs` and (after
  this) `brainstorm-round.mjs` need it; the audit script's `modelPricing`
  approach is the prior art and brainstorm should mirror it locally.

## 9. Acceptance Criteria

Two tiers (R1-M7): **repo-verifiable** (CI-green from a fresh clone) and
**local-smoke** (depends on user's home directory or installed tools).

### 9a. Repo-verifiable (gating)

| ID | Criterion | Verifiable by |
|---|---|---|
| AC1 | `skills/brainstorm/SKILL.md` exists in audit-loop | `test -f skills/brainstorm/SKILL.md` |
| AC2 | `scripts/brainstorm-round.mjs` exists and is executable Node CLI | `node scripts/brainstorm-round.mjs --help \| grep -q brainstorm` |
| AC3 | Brainstorm tests pass | `npm test -- --test-name-pattern brainstorm` exits 0 |
| AC4 | Concept-level prompt lives in `scripts/lib/brainstorm/prompt.mjs` (single source of truth — R2-L1) | `grep -q "thoughtful brainstorming partner" scripts/lib/brainstorm/prompt.mjs && ! grep -q "thoughtful brainstorming partner" skills/brainstorm/SKILL.md` |
| AC5 | SKILL.md instructs explicit STOP after presenting views | `grep -q "STOP" skills/brainstorm/SKILL.md && grep -q "Only When Asked" skills/brainstorm/SKILL.md` |
| AC6 | sync-to-repos.mjs lists brainstorm helper in CORE_SCRIPTS | `grep -q "brainstorm-round.mjs" scripts/sync-to-repos.mjs` |
| AC7 | Brainstorm helper uses `resolveModel()` (no hardcoded model IDs) | `grep -q "resolveModel" scripts/brainstorm-round.mjs && ! grep -E "['\"](gpt-[0-9]\|gemini-[0-9])" scripts/brainstorm-round.mjs` |
| AC8 | Brainstorm helper redacts secrets before sending | `grep -q "redactSecrets\\|secret-patterns" scripts/brainstorm-round.mjs` |
| AC9 | No `--no-redact` flag exists in helper (R2-H3) | `! grep -q -- "--no-redact" scripts/brainstorm-round.mjs` |
| AC10 | Helper supports `misconfigured` provider state (R2-H2) | `grep -q "'misconfigured'" scripts/lib/brainstorm/schemas.mjs` |
| AC11 | Helper output contract is total — JSON written even when all providers fail (R2-H4) | Test case in `tests/brainstorm-round.test.mjs`: mock both adapters to throw; assert helper exits 0 AND `--out` file is schema-valid JSON with both `state != 'success'` |
| AC12 | Malformed-payload paths use `os.tmpdir()` not hardcoded `/tmp/` (R2-M3) | `grep -q "os.tmpdir\\|tmpdir()" scripts/brainstorm-round.mjs && ! grep -E "['\"](/tmp/)" scripts/brainstorm-round.mjs` |
| AC13 | All 3 repos' `README.md` contain the discoverability marker | `for r in claude-audit-loop wine-cellar-app ai-organiser; do grep -q "arch-map-discoverability:start" /c/GIT/$r/README.md; done` |
| AC14 | All 3 repos' `AGENTS.md` contain the discoverability marker | Same pattern as AC13 against AGENTS.md |
| AC15 | All 3 repos have committed `.vscode/extensions.json` with `bierner.markdown-mermaid` | `for r in ...; do jq -e '.recommendations \| index("bierner.markdown-mermaid")' /c/GIT/$r/.vscode/extensions.json; done` |
| AC16 | ai-organiser `.gitignore` uses `.vscode/*` not `.vscode/` and contains `!.vscode/extensions.json` | `grep -qx ".vscode/\\*" /c/GIT/ai-organiser/.gitignore && grep -qx "!.vscode/extensions.json" /c/GIT/ai-organiser/.gitignore` |
| AC17 | ai-organiser `git check-ignore -v .vscode/extensions.json` exits 1 (NOT ignored) | `cd /c/GIT/ai-organiser && ! git check-ignore -q .vscode/extensions.json` |
| AC18 | ai-organiser `git check-ignore -v .vscode/mcp.json` exits 0 (still ignored) | `cd /c/GIT/ai-organiser && git check-ignore -q .vscode/mcp.json` |
| AC19 | Re-running discoverability insertion is idempotent (marker check) | Manual: re-trigger insert on already-edited file; assert no second occurrence of `arch-map-discoverability:start` |
| AC20 | Plan §7 contains the architectural-memory neighbourhood consultation block (R2-M2) | `grep -q "Neighbourhood considered\\|get-neighbourhood" docs/plans/brainstorm-and-arch-discoverability.md` |
| AC21 | Helper guards against `--topic` and `--topic-stdin` both supplied (R3-H1) | Test in `tests/brainstorm-round.test.mjs`: argv `--topic foo --topic-stdin` → exit 1 |
| AC22 | Consumer repos have the synced helper after rollout (R3-H4) | `for r in wine-cellar-app ai-organiser; do test -f /c/GIT/$r/scripts/brainstorm-round.mjs && test -f /c/GIT/$r/scripts/lib/brainstorm/openai-adapter.mjs; done` |
| AC23 | Provider order in JSON output matches `--models` argv order (R3-M2) | Test in `tests/brainstorm-round.test.mjs`: with both providers mocked to succeed at different latencies, assert `providers[].provider` order matches argv |
| AC24 | SKILL.md uses temp-file pattern for topic stdin (Gemini-G1 v1+v2 — no heredoc, no shell interpolation) | `grep -q ".claude/tmp/brainstorm" skills/brainstorm/SKILL.md && grep -q -- "--topic-stdin" skills/brainstorm/SKILL.md && ! grep -q "EOF_BRAINSTORM" skills/brainstorm/SKILL.md && ! grep -E "printf.*\\\\\"\\\\\\$" skills/brainstorm/SKILL.md` |
| AC25 | Helper exposes `resolvedModels` field in output for post-call rendering (Gemini-G2 v1) | `grep -q "resolvedModels" scripts/lib/brainstorm/schemas.mjs && grep -q "resolvedModels" scripts/brainstorm-round.mjs` |
| AC26 | Helper writes malformed-payload debug files with mode 0o600 inside `.claude/tmp/` (Gemini-G3) | `grep -q "mode: 0o600\|mode:0o600" scripts/brainstorm-round.mjs && grep -q "\.claude/tmp" scripts/brainstorm-round.mjs && ! grep -q "os.tmpdir" scripts/brainstorm-round.mjs` |
| AC27 | Cost preflight estimate accounts for input AND output tokens (Gemini-G2 v2) | `grep -q "input_chars\|inputTokens" scripts/brainstorm-round.mjs && grep -q "estimatedCostUsd\|preflight" scripts/brainstorm-round.mjs` |
| AC28 | `.claude/tmp/` is gitignored in audit-loop and synced to consumers | `grep -q "\.claude/tmp" .gitignore && grep -q "\.claude/tmp" scripts/sync-to-repos.mjs` |

### 9b. Local-smoke (informational, not CI-gating)

| ID | Criterion | Verifiable by |
|---|---|---|
| LS1 | Live integration: `node scripts/brainstorm-round.mjs --topic test --models openai` returns `state: success` | Run with real `OPENAI_API_KEY`; assert `.providers[0].state == "success"` |
| LS2 | Live integration: missing key surfaces as `misconfigured` (R2-H2) | `unset OPENAI_API_KEY; node scripts/brainstorm-round.mjs --topic test --models openai`; helper exits 0; JSON has `providers[0].state == "misconfigured"` |
| LS3 | Live integration: both providers configured returns 2 success blocks | `node scripts/brainstorm-round.mjs --topic test --models openai,gemini`; both providers `state: success` |
| LS4 | Mermaid extension prompt fires on fresh VS Code workspace open in each repo | Manual: open each repo cold; observe extension recommendation toast |
