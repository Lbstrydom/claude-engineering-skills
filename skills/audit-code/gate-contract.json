{
  "version": 1,
  "skill": "audit-code",
  "gates": [
    {
      "id": "convergence-threshold",
      "kind": "executable",
      "oracle": "convergence-threshold",
      "statedIn": "skills/audit-code/SKILL.md",
      "stated": "Quality threshold: `HIGH == 0 && MEDIUM <= 2 && quickFix == 0`",
      "implementation": "scripts/lib/audit/convergence.mjs",
      "tests": [
        "tests/gate-honesty.test.mjs"
      ],
      "params": {
        "high": 0,
        "medium": 2,
        "quickFix": 0
      },
      "proof": "unit-seam"
    },
    {
      "id": "tiered-shadow-window-honesty",
      "kind": "executable",
      "oracle": "tiered-shadow-window",
      "statedIn": "AGENTS.md",
      "stated": "`comparedRuns` counts only rows the collector stamped with the\n  current `TIERED_SHADOW_CONTRACT_EPOCH`",
      "implementation": "scripts/lib/audit/tiered-shadow-summary.mjs",
      "tests": [
        "tests/gate-honesty.test.mjs"
      ],
      "fixture": {
        "rows": [
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          },
          {
            "legacyOk": true,
            "shadowOk": false,
            "comparison": {
              "tieredRunStatus": "fallback_legacy"
            }
          }
        ]
      },
      "proof": "unit-seam"
    },
    {
      "id": "mechanical-vs-architectural-label",
      "kind": "document-only",
      "reason": "is_mechanical is a GPT judgement call per finding; only the RESET RULE given that label is mechanical, not the label-assignment itself",
      "statedIn": "skills/audit-code/SKILL.md",
      "stated": "Mechanical vs architectural"
    },
    {
      "id": "rigor-pressure-stop",
      "kind": "document-only",
      "reason": "deciding whether a re-raised finding is 'genuinely new' vs a stability-plateau rehash is a judgement call on GPT's part; no mechanical oracle exists for the classification itself",
      "statedIn": "skills/audit-code/SKILL.md",
      "stated": "Threshold met, 0 new, 2/2 stable"
    },
    {
      "id": "detector-blocks-convergence",
      "kind": "document-only",
      "statedIn": "skills/audit-code/SKILL.md",
      "stated": "Re-run every detector at FULL scope (blocks convergence)",
      "reason": "Step 5.0b's claim IS mechanically enforced — evaluateConvergenceWithDetectors (scripts/lib/audit/convergence.mjs) returns converged:false with reason 'detector-undispositioned' when any match is undispositioned, and reason 'detector-not-run' when the caller supplies no detector result at all; both are pinned in tests/audit-detector.test.mjs, along with the full-scope property and the unverifiable states (globs matching no file, ripgrep unavailable). It is document-only because the CLOSED oracle registry has no fit: convergence-threshold binds only the finding-count params, cli-exit drives a CLI against a filesystem fixture, and the detector gate's subject is a ledger-plus-repo state a recipe cannot construct. Claiming an oracle that does not bind would be the fake-check class this suite exists to catch. Revisit if a ledger-fixture oracle is ever added."
    }
  ],
  "ignoredCandidates": [
    {
      "line": "# as an ARGUMENT (process.argv[1]), never embed it as a literal string inside",
      "reason": "Comment instructing future editors of this Step 7 snippet not to reintroduce a literal /tmp path string for Node to independently re-resolve. The actual invariant is mechanically enforced repo-wide by tests/skill-tmp-path-safety.test.mjs (scans every skills/**/*.md for require/readFileSync/existsSync called on a string-literal path), not by anything this skill's own execution exits on."
    },
    {
      "line": "# Gemini verdict computed (visible in that session's own logs) but NEVER",
      "reason": "Incident narrative (wine-cellar-app's 101 lost final-review verdicts) explaining WHY this snippet was rewritten; not itself a new rule. Same already-covered no-literal-tmp-path invariant as the candidate above."
    },
    {
      "line": "# error. fs.readFileSync + JSON.parse (not require — this is a data file, not a",
      "reason": "Continuation of the same incident-narrative comment, explaining the fs.readFileSync/JSON.parse replacement for require(); no new claim beyond the no-literal-tmp-path invariant already covered above."
    },
    {
      "line": "# module) with a try/catch fails safe to an empty RUN_ID rather than crashing.",
      "reason": "Continuation of the same comment block — describes the try/catch already visible on the snippet's own next line, not a separate enforceable claim."
    },
    {
      "line": "RUN_ID=$(node -e \"const fs=require('fs'); try { process.stdout.write(JSON.parse(fs.readFileSync(process.argv[1],'utf8'))._cloudRunId||''); } catch { process.stdout.write(''); }\" \".audit/$SID-r<N>-result.json\")",
      "reason": "The corrected extraction line itself — its correctness (argv-based path, never a literal string) is exactly what tests/skill-tmp-path-safety.test.mjs asserts across every skill markdown file. This is a code snippet a human/agent runs inline, not a CLI /audit-code itself exits on, so there is no separate cli-exit oracle to bind here beyond that regression test."
    },
    {
      "line": "**Structured, never a shell command.** `pattern`/`globs` reach ripgrep as argv. The",
      "reason": "A property of the detector SCHEMA (DetectorSchema is a closed z.literal(\"regex\") union with no field a command string fits into), not a gate the skill enforces at run time. The shape is asserted in tests/audit-detector.test.mjs; \"never a shell command\" describes what is unrepresentable, and there is no run to fail."
    },
    {
      "line": "**Dispositions key on `<path>::<trimmed matched line>`**, never a line number — a line",
      "reason": "A FORMAT statement about the disposition key, explaining why it is not a line number (a line number orphans on any insertion above the match). matchKey() is the single producer and its identity properties — survives a line shift, distinguishes identical text in one file — are pinned in tests/audit-detector.test.mjs. Not a separate gate: the enforced claim is detector-blocks-convergence above."
    },
    {
      "line": "blocked; exit 2 = the ledger could not be read, which is \"no census happened\"",
      "reason": "Documents the exit mapping of the `scripts/lib/audit/detector.mjs` CLI the operator runs (it replaced an inline `node -e` snippet on 2026-08-08, which the consumer sync's command rewriter could not relocate). The exit codes are the CLI's own, asserted in tests/audit-detector.test.mjs; the claim /audit-code itself enforces is detector-blocks-convergence above, not this line."
    },
    {
      "line": "convergence requires `blocked === false`. `baseline` reports progress; it is never the pass",
      "reason": "Restates detector-blocks-convergence and adds the negative half — that `baseline` is reporting only. Both are the same contracted claim, and the baseline-is-not-the-pass-condition property is pinned directly in tests/audit-detector.test.mjs."
    },
    {
      "line": "Run → **confirm RED**, and read the failure text: it must name the actual",
      "reason": "Step 4.5 instruction to the AGENT performing the two-direction proof: run the reverted fix and READ the failure rather than accepting any red. Agent behaviour with no oracle — nothing in /audit-code can assert that the fix was actually reverted, and claiming an executable binding here would be the fake-check this suite exists to catch."
    },
    {
      "line": "defect, not fail for an unrelated reason.",
      "reason": "Continuation of the same Step 4.5 sentence. States what a VALID red looks like (fails for the defect, not a 404 or a boot error). A judgement about why a test failed; no oracle in the closed registry classifies a failure reason."
    },
    {
      "line": "passing, and an always-passes probe is indistinguishable from a correct one.",
      "reason": "An explanatory claim about probes in general — the reason a before/after observation is not a negative control. It describes epistemics, not a gate /audit-code enforces; there is no exit code for 'this probe could have failed'."
    },
    {
      "line": "assertion proving the probe can find something it must find.",
      "reason": "The vacuous-pass-guard instruction. Whether an added assertion genuinely exercises the probe is exactly the judgement the guard exists to force; a mechanical oracle for 'this assertion is non-vacuous' is precisely what does not exist."
    },
    {
      "line": "**When the check itself fails, suspect the instrument first.** In one measured",
      "reason": "Failure-triage guidance for the agent (check the instrument before the subject). Ordering advice about where to look first, not a gate — /audit-code produces no exit code that could assert an investigator followed it."
    },
    {
      "line": "| `references/verification-discipline.md` | Verification discipline — pinned citations, figure provenance, two-direction proof, attribution, consumer-side checks. | Step 4.5 — a finding was fixed WITH a new test or guard and it must be proven red-then-green, OR authoring a contract test from a one-off check. |",
      "reason": "A REFERENCE-TABLE row. The 'Read when' cell describes when to open a reference file; it asserts nothing this skill enforces and produces no exit code. The enforcement verb is incidental to a trigger description."
    },
    {
      "line": "> **Precondition: the ledger must already carry per-finding rulings.** This CLI",
      "reason": "Step 3.5b documents an INPUT precondition of write-code-outcomes.mjs, not a gate the skill enforces. The CLI records rulings and cannot make them; with an un-adjudicated ledger it labels nothing and exits 0 deliberately (telemetry must never fail a run). What it now does — name which of three causes applies, on stderr and as `warning` on the JSON — is advisory output, with no exit code for a cli-exit recipe to bind. All three diagnosis branches were exercised live on 2026-08-08 (empty ledger / all-pending / ruled-but-unjoined)."
    },
    {
      "line": "> and exits 0 — success-shaped, having closed nothing (reported live",
      "reason": "Continuation of the same precondition block: the narrative of the reported defect (0/N labelled reading as success). Not a separate claim, and deliberately not a new exit code — a telemetry CLI that fails a run is worse than one that warns."
    },
    {
      "line": "> non-zero `total` is a failure to fix, never a pass.**",
      "reason": "An instruction to the AGENT reading the CLI output: do not treat labelled:0 as a closed loop. Agent judgement about a reported number, with no mechanical seam — the CLI cannot know whether the operator intended to adjudicate this round."
    },
    {
      "line": "Exit 0 = every detector reached zero and every match is dispositioned; exit 1 =",
      "reason": "Documents the exit mapping of the scripts/lib/audit/detector.mjs CLI that replaced Step 5.0b's inline node -e snippet on 2026-08-08 (the snippet form could not be relocated by the consumer sync, so it ran in no consumer). The mapping is the CLI's own and is asserted directly in tests/audit-detector.test.mjs ('the detector CLI maps its three outcomes onto distinct exit codes': clean→0, unverifiable→1, unreadable/no-arg→2). The claim /audit-code itself enforces remains detector-blocks-convergence above; this line describes the tool it calls."
    },
    {
      "line": "and never a pass.",
      "reason": "Tail of the same exit-mapping sentence — exit 2 (unreadable ledger) is \"no census happened\", which must not read as clean. Same single claim, same test."
    },
    {
      "line": "writes. Never hand-assemble the JSON:",
      "reason": "Step 7 instruction to run build-audit-transcript.mjs rather than hand-rolling the transcript. It constrains what the AGENT does before invoking the gate; nothing in /audit-code exits non-zero on a hand-written transcript (gemini-review accepts any JSON by design, which is why the builder exists). The builder's own refusals — no rounds → exit 1, unresolvable mode → exit 2 — are asserted in tests/audit-transcript-build.test.mjs."
    }
  ]
}
