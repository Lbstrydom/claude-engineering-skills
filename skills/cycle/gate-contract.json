{
  "version": 1,
  "skill": "cycle",
  "gates": [
    {
      "id": "cluster-start-ref-validated-on-use",
      "kind": "document-only",
      "statedIn": "skills/cycle/SKILL.md",
      "stated": "halt and report rather than demoting to the\n     dirty-aware default",
      "reason": "DOCUMENT-ONLY here, but the claim is NOT unbacked — the mechanical half lives one layer down and is tested. /cycle is an orchestrator that delegates; it emits no exit code of its own, so there is nothing for the cli-exit oracle to assert against, and asserting one would be the fake-check class this registry exists to catch. What IS enforced: scripts/lib/worktree-identity.mjs::resolveRangeSnapshot refuses a non-ancestor explicit base with reason 'not-an-ancestor', and scripts/openai-audit.mjs re-raises that refusal PAST the catch that would otherwise swallow it into a silent fallback to plan-referenced files — the precise demotion this SKILL line forbids. Pinned by tests/audit-base-ancestry.test.mjs (against a real diverged ref) and tests/diff-base-resolver.test.mjs (injected runner). So the SKILL prose is the first of two layers and says so; the second layer is where the exit code is. Added 2026-08-11 under docs/plans/worktree-identity-guards.md Phase 5."
    },
    {
      "id": "preview-gate-halt-blocks-ship",
      "kind": "document-only",
      "reason": "'Under [HALT] a P0 blocks ship until fixed' delegates to the preview-gate seam (cross-skill.mjs preview-gate) whose decision is a tested function, but /cycle enacts the halt by ORCHESTRATION — it reads the directive and chooses not to ship. There is no /cycle CLI exit code; /cycle is an orchestrator invoking other skills, so the halt is agent flow, not a mechanical exit a cli-exit recipe can assert."
    },
    {
      "id": "fix-gate-convergence-before-next-cluster",
      "kind": "document-only",
      "reason": "'fix-gate: yes → reach /audit-code convergence (HIGH==0 && MEDIUM<=2 && quickFix==0) before the next cluster builds on it' is the SAME predicate audit-code's contracted convergence-threshold gate already binds. /cycle enforces it by orchestration (it runs /audit-code and reads the verdict), so there is no separate /cycle exit code — the executable binding lives in audit-code's contract, and /cycle's use of it is document-only."
    },
    {
      "id": "author-tier-never-routes",
      "kind": "document-only",
      "reason": "'AUDIT_AUTHOR_TIER_HINT is observation-only — /cycle records it, never routes on it, no wrong tier hint has any execution impact' is a NEGATIVE guarantee (no routing code reads it to change which model runs). Asserting an absence-of-routing is a source-graph negative with no cli-exit binding; there is no router to exercise. Document-only."
    },
    {
      "id": "consolidated-gemini-gate-mandatory",
      "kind": "document-only",
      "reason": "'The consolidated Gemini gate is mandatory after clustered execution, regardless of per-cluster convergence' is an orchestration invariant /cycle enacts by always running the union-diff Gemini review. No /cycle CLI, no exit code — the mandatory-ness is agent flow the model performs, not a mechanical gate."
    }
  ],
  "ignoredCandidates": [
    {
      "line": "same base (`not-an-ancestor`, hard fail), which is where the exit code is.",
      "reason": "Continuation of the same sentence; same disposition. \"hard fail\" describes /audit-code's behaviour, not a gate /cycle enforces."
    },
    {
      "line": "This is the first of two layers: `/audit-code`'s own resolver refuses the",
      "reason": "Points at WHERE the enforcement lives rather than asserting one here: /cycle is an orchestrator with no exit code of its own. The refusal named is scripts/lib/worktree-identity.mjs::resolveRangeSnapshot returning not-an-ancestor, contracted under cluster-start-ref-validated-on-use above and asserted in tests/audit-base-ancestry.test.mjs against a real diverged ref."
    },
    {
      "line": "an existing plan or straight to code-audit, per-step skips, max-round",
      "reason": "Usage/description documentation line surfaced by the 2026-07-21 Copilot-cap description trim (Usage relocated to the SKILL body). Not a new gate claim — it documents an existing flag; the real gate (where one exists, e.g. nav-audit --gate) is contracted separately in this file."
    },
    {
      "line": "Usage: /cycle <plan-file> --max-rounds N  — Pass through to /audit-plan and /audit-code",
      "reason": "Usage/description documentation line surfaced by the 2026-07-21 Copilot-cap description trim (Usage relocated to the SKILL body). Not a new gate claim — it documents an existing flag; the real gate (where one exists, e.g. nav-audit --gate) is contracted separately in this file."
    },
    {
      "line": "clean pass; it means the run never reached the app's primary",
      "reason": "Part of Step 5's new guidance on reading persona-test's authWallUntested flag: /cycle reads the persona-test report and decides not to treat 0-P0-with-untested-auth-surface as a clean pass. Same category as the already-contracted 'preview-gate-halt-blocks-ship' gate — /cycle is an orchestrator with no CLI exit code of its own; this is agent flow reading another skill's report, not a mechanical gate."
    },
    {
      "line": "**Fix-gate**: `fix-gate: yes` → reach `/audit-code` convergence (`HIGH==0 && MEDIUM<=2 && quickFix==0`) **over this cluster's IN-CLUSTER findings** (see 4a) before the next cluster; `none` skips; `final` defers to the consolidated gate. Within-cluster fixing is authorized; a fix needing files **outside** the cluster's scope → **stop** for confirmation (mark any touched `gate-clear` cluster `stale`). Persistent non-convergence → hand back with a summary.",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "**4a. The gate scores IN-CLUSTER findings only — otherwise no cluster but the last can ever pass.** `/audit-code` grades the cluster's *diff* against the *whole plan*, so a later cluster's not-yet-written file comes back as `[Structure] Missing planned file` — **HIGH**. Measured 2026-08-08 (`gate-honesty-adjudicated-defects.md` §11.1): a cluster's total HIGH went 6 → 5 → 3 while its in-cluster HIGH went 2 → 2 → **0**; at that point its own work was clean and the gate would have refused *forever*, on two files belonging to clusters correctly not yet implemented. A gate that cannot be satisfied by doing the work correctly is the cried-wolf shape that earns `--no-verify`. Classify every finding by the cluster owning its cited file — the derived scopes are already computed, so this is mechanical, not a judgement call:",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "| Cited file is in… | Class | Effect on THIS gate |",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "| this cluster's derived scope | `in-cluster` | **gates** — fix it, pre-existing or not |",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "| a **later** cluster's derived scope | `deferred-declared` | does not gate; **must be zero at the final gate** |",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "| an **earlier** cluster's derived scope | `regression` | **gates** — that cluster is now `stale`; go to step 5 |",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "| **no** cluster's derived scope | `out-of-scope` | unchanged: fail closed per step 3 |",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "Three properties stop this becoming an escape hatch, and all three are load-bearing: **(1)** deferral is *bounded* — `deferred-declared` means \"owned by a LATER cluster\", and the final cluster has none, so the bucket is empty there by construction and every deferred completeness finding must be satisfied before Step 3C.2; **(2)** *\"pre-existing\" is still not a defer reason* — these buckets are about plan SCHEDULE, never authorship, so AGENTS.md's impact test applies unchanged to anything in-cluster; **(3)** the classification is *derived*, never argued — a finding cannot be talked into a friendlier bucket. **Every `deferred-declared` finding is listed in the cluster's hand-back summary with the cluster that owns it** — a deferral nobody can see is indistinguishable from a dismissal.",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "`deferred-declared` finding accumulated across the run: each must now be",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "**The consolidated Gemini gate is mandatory** after clustered execution, regardless of per-cluster convergence — and Step 3C.2's deferral re-check runs *before* it, so no `deferred-declared` finding survives the run.",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    },
    {
      "line": "**A per-cluster fix-gate scores IN-CLUSTER findings only** (Step 4a). Scoring the whole plan against one cluster's diff makes every gate but the last unpassable; deferral is bounded to *later*-cluster scopes and settled at 3C.2. Never widen it to \"pre-existing\" — that is authorship, not schedule.",
      "reason": "Step 3C in-cluster fix-gate rule (2026-08-08). /cycle is a THIN ORCHESTRATOR: the loop is executed by the model, not by code, so none of these lines claims an enforcing script — they instruct the agent, and the enforcement they describe belongs to /audit-code (convergence thresholds, contracted in skills/audit-code/gate-contract.json) and to gemini-review.mjs (the consolidated gate). Contracting them here would assert an oracle that does not exist, which is the fake-check class this registry exists to catch. Added when the fix-gate wording was corrected: the old text demanded whole-plan convergence per cluster, which no cluster but the last could reach (measured 6->5->3 total HIGH against 2->2->0 in-cluster). See docs/plans/gate-honesty-adjudicated-defects.md 11.1."
    }
  ]
}
