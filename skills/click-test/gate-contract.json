{
  "version": 1,
  "skill": "click-test",
  "gates": [
    {
      "id": "verdict-precedence",
      "kind": "document-only",
      "reason": "'The overall verdict becomes at most Incomplete (never Clean) when any route is auth-required' is computed inside the click-test scanner logic the AGENT runs via Playwright MCP page.evaluate — click-test has no CLI entry point (verified: no scripts/click-test*.mjs), so there is no exit code a cli-exit recipe can assert, and the verdict function is not one of the closed registry's hardwired unit-seam oracles."
    },
    {
      "id": "arg-validation-refusals",
      "kind": "document-only",
      "reason": "The argument refusals — unknown device name -> fail, supplying two device flags -> fail, unknown flags -> fail, missing url -> usage + STOP, invalid scope -> fail — are enacted by the AGENT parsing the /click-test invocation, not by a CLI with an exit code. click-test drives a browser via MCP and has no script to spawn; these are agent-behavioural refusals with no cli-exit binding."
    },
    {
      "id": "capability-abort",
      "kind": "document-only",
      "reason": "'If any required capability is missing, abort before scanning' and the WebFetch-only degradation are decisions the model makes about which MCP tools are available before it drives the browser — runtime capability discovery, not a script exit code. No oracle in the closed registry binds an MCP-capability precondition."
    },
    {
      "id": "scanner-error-caps",
      "kind": "document-only",
      "reason": "The >999-findings -> scanner-error cap (a Zod .max(999) inside the scanner) and the failed-validation -> scanner-error classification live in code the agent runs via page.evaluate against the live DOM; they surface as a coverageStatus field, not a process exit code. Assertable only by importing the scanner into a browser context, which the closed cli-exit/unit-seam registry does not cover."
    }
  ],
  "ignoredCandidates": [
    {
      "line": "click-test itself never attempts to log in — credentials are out of scope",
      "reason": "Restates click-test's existing scope boundary (credentials out of scope) in the new auth-bootstrap cross-link paragraph. Same agent-behavioural-refusal category as the already-contracted 'arg-validation-refusals' gate — click-test has no CLI to exit, so this is a rule the model follows while running the skill, not a mechanical gate."
    },
    {
      "line": "becomes at most `Incomplete` (never `Clean`) when any route is",
      "reason": "This is the same claim already covered by the 'verdict-precedence' document-only gate above — the surrounding paragraph was rewritten to add the auth-bootstrap cross-link, which shifted this sentence onto a new diff line without changing its meaning. 'verdict-precedence' already explains why no cli-exit/unit-seam oracle applies (no click-test CLI entry point)."
    },
    {
      "line": "declaredSeverity: enum(\"P0\",\"P1\",\"P2\",\"P3\"),  // the kind's intrinsic severity, pre-cap",
      "reason": "A field in the pseudo-Zod finding contract, not a gate claim. The cap it records IS mechanically enforced in push() and asserted per-kind by tests/click-test-perceivability.test.mjs against a real browser; this line only names the field that preserves the pre-cap value."
    },
    {
      "line": "### Perceivability cap (2026-07-30)",
      "reason": "A section heading. The behaviour it introduces is enforced by the single push() call site and covered by tests/click-test-perceivability.test.mjs (visible P0 stays P0; hidden file input demotes to P3; the cap applies across kinds)."
    },
    {
      "line": "tag is **state-relative**: it says \"not perceivable in *this* capture\", never",
      "reason": "Documents how a human should READ the perceivable flag — an interpretation caveat, deliberately not a mechanical claim. Nothing can assert \"the reader understood this is state-relative\"; the mechanical half (that a finding is demoted rather than dropped, so a later --with-modals scan can re-report it) is tested."
    },
    {
      "line": "\"never perceivable\".",
      "reason": "The continuation line of the state-relative caveat above; same disposition — an interpretation rule for the reader, not an enforceable gate."
    },
    {
      "line": "> **Scope note — this is NOT a release-gate fix.** `/ship` does **not** gate on",
      "reason": "A NEGATIVE claim recording that no /ship gate consumes click-test P0s (persistence is deferred in Phase 7; no record-click-test subcommand, table or read path exists). It exists precisely to stop a future reader re-asserting the false release-gate premise a prior audit round disproved. There is no mechanism to contract — the honest gate would be the absence of one."
    },
    {
      "line": "> cannot turn a cosmetic wrong into a real false gate.",
      "reason": "Continuation of the scope note above — states why the severity fix is preventive ahead of the deferred v2 persistence. Same disposition: a rationale for sequencing, not an enforceable claim."
    }
  ]
}
