{
  "version": 1,
  "skill": "nav-audit",
  "gates": [
    {
      "id": "exit-2-tool-error",
      "kind": "executable",
      "oracle": "cli-exit",
      "scenario": "nav-invalid-contract",
      "statedIn": "skills/nav-audit/SKILL.md",
      "stated": "`2` tool",
      "implementation": "scripts/nav-audit.mjs",
      "tests": [
        "tests/gate-honesty.test.mjs"
      ],
      "proof": "process"
    },
    {
      "id": "bootstrap-refuses-to-clobber",
      "kind": "executable",
      "oracle": "cli-exit",
      "scenario": "nav-bootstrap-refuse-clobber",
      "statedIn": "skills/nav-audit/SKILL.md",
      "stated": "refuses to clobber",
      "implementation": "scripts/nav-audit.mjs",
      "tests": [
        "tests/gate-honesty.test.mjs"
      ],
      "proof": "process"
    },
    {
      "id": "gate-exit-1-on-regression",
      "kind": "document-only",
      "reason": "'With --gate, exit 1 only when a declared-intent regression appears on the changed surface' needs a fixture with an observed nav graph, a nav-contract.json declaring intent, AND a git diff producing a genuine regression on the changed surface — a multi-artifact setup that also depends on the diff-scope resolver. Constructing that deterministically is beyond a minimal cli-exit fixture; the two exit-2 tool-error paths are bound executably above, and this exit-1 path is document-only until (if ever) it earns a dedicated fixture."
    }
  ],
  "ignoredCandidates": [
    {
      "line": "> \"skills:hydrate\": \"node -e \\\"const{execFileSync}=require('node:child_process'),p=require('node:path'),f=require('node:fs');const main=p.dirname(execFileSync('git',['rev-parse','--path-format=absolute','--git-common-dir'],{encoding:'utf8'}).trim());const dir='scripts/.claude-skills';const src=p.join(main,dir);if(p.resolve(dir)===p.resolve(src)){console.log('[hydrate] main checkout - nothing to do');process.exit(0)}if(!f.existsSync(src)){console.error('[hydrate] no tooling at '+src+' - re-sync the main checkout first');process.exit(1)}f.cpSync(src,dir,{recursive:true});console.log('[hydrate] copied '+src)\\\"\"",
      "reason": "The worktree-preflight marker block's inlined `skills:hydrate` recipe. Every node one-liner carries `require` and `process.exit`, two ENFORCEMENT_VERBS, and no phrasing of a runnable recipe avoids them — the verbs are JavaScript here, not an enforcement claim about this skill. The recipe is inlined rather than cited because in an unadopted consumer both of the old pointers are dead at once (no `skills:hydrate` script yet, and `docs/runbooks/` is not synced), so a skill file is the only carrier that reaches the reader. Byte-identical across all 16 contracts by construction: it is generated from CONSUMER_HYDRATE_NPM_SCRIPT via MARKER_BLOCK, and `checkSkill` pins the block in every copy."
    },
    {
      "line": "registry), and gates CI only on declared-intent regressions. A verify mode",
      "reason": "Usage/description documentation line surfaced by the 2026-07-21 Copilot-cap description trim (Usage relocated to the SKILL body). Not a new gate claim — it documents an existing flag; the real gate (where one exists, e.g. nav-audit --gate) is contracted separately in this file."
    },
    {
      "line": "/nav-audit --gate                  — exit non-zero on a declared-intent regression on the changed surface",
      "reason": "Usage/description documentation line surfaced by the 2026-07-21 Copilot-cap description trim (Usage relocated to the SKILL body). Not a new gate claim — it documents an existing flag; the real gate (where one exists, e.g. nav-audit --gate) is contracted separately in this file."
    },
    {
      "line": "v1.5. Declare in `nav-contract.json` something that must be observable when a",
      "reason": "Authoring guidance for the optional authSentinel field. Its schema is enforced by NavContractSchema (scripts/lib/nav/schema.mjs, .strict()), and the liveness truth table it introduces is covered by tests/nav-verify.test.mjs (composeCaptureVerdict across all states). This line instructs the AUTHOR what to declare; it is not itself a gate."
    },
    {
      "line": "| yes | declared | selector error | `unverified` | Authoring bug — never reported as `dead` |",
      "reason": "One row of the authLiveness truth table. The invariant it states (a selector that fails to evaluate degrades to `unverified`, never `dead`, so an authoring bug cannot masquerade as an expired token) is implemented in runVerify (sentinelError branch) and asserted in tests/nav-verify.test.mjs."
    },
    {
      "line": "persisted verify result correctly reads stale and must be re-run.",
      "reason": "Describes an existing, already-contracted freshness mechanism: authSentinel joins computeContractDigest, and NAV_VERIFY_TOOL_VERSION was bumped 2→3, so readVerifyResult rejects a pre-bump envelope. Covered by tests/nav-verify-store.test.mjs; this line restates that consequence for the operator."
    }
  ]
}
