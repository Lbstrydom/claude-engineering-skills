{
  "version": 1,
  "skill": "ship",
  "gates": [
    {
      "id": "guard-a-unscoped-commit-refused",
      "kind": "executable",
      "oracle": "cli-exit",
      "scenario": "ship-unscoped-index-refusal",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "`--path` is MANDATORY, not conditional",
      "implementation": "scripts/ship-commit.mjs",
      "tests": [
        "tests/gate-honesty.test.mjs"
      ],
      "proof": "process"
    },
    {
      "id": "guard-b-identity-is-a-precondition",
      "kind": "executable",
      "oracle": "cli-exit",
      "scenario": "ship-identity-absent-refusal",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "Both of these are now REQUIRED, and `ship-commit` refuses without them",
      "implementation": "scripts/ship-commit.mjs",
      "tests": [
        "tests/gate-honesty.test.mjs"
      ],
      "proof": "process"
    },
    {
      "id": "gate-passed-refused-without-evidence",
      "kind": "document-only",
      "reason": "'--gate passed is refused unless the run's convergence verdict is verified against the cloud store' is real (ship-commit.mjs enforces it — observed live this session), but its exit code depends on the git + .audit/last-audit-run.json evidence state (exit 1 vs 2 across setups), so a hermetic cli-exit fixture cannot bind it DETERMINISTICALLY — the trigger state is subtle and store-dependent. The refusal itself is verified by ship-commit's own provenance tests; document-only here rather than a fragile recipe."
    },
    {
      "id": "gate-converged-requires-verified-divergence",
      "kind": "document-only",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "a non-converged run refuses either and leaves `waived`",
      "reason": "The `stated` is a VERBATIM quote from skills/ship/SKILL.md, which is the contract for this field — a first draft paraphrased the claim instead, so the entry asserted that text appeared in a file where it did not. A DISTINCT entry rather than a widening of gate-passed-refused-without-evidence, because that entry's claim is specifically about passed's evidence requirement and merging the two would blur claims that must stay independently traceable. `converged` (added 2026-09-04, docs/plans/gate-taxonomy-remediated-ships.md) is the equal/differing sibling of passed over the same evidence: it clears the SAME store bar — cloud off, run not found, or roundConvergedAfter == null all refuse it — and additionally requires committedTree !== evidence.auditedTree, refusing the equal-tree case as an under-claim and naming passed. Document-only for exactly the reason its sibling is: the refusal is real and enforced in scripts/lib/commit-trailers.mjs::evaluateGateVerification, but its exit code depends on the git + .audit/last-audit-run.json evidence state, so a hermetic cli-exit fixture cannot bind it deterministically — and the cli-exit oracle's scenario enum is closed, with no member for this state. The refusals themselves ARE mechanically covered: tests/commit-trailers.test.mjs pins the accept plus five refusal directions (equal tree, non-converged run, cloud off, unresolvable comparand, no audited-tree identity) and the AI-Audited-Tree omission, and tests/ship-commit-cli.test.mjs rows 5c/5d pin the end-to-end refusal and the --no-tests cap. Those bindings are not declared as fields here because DocumentOnlyGateSchema is .strict() and permits only {id, kind, reason, statedIn, stated}; claiming an implementation/tests binding this schema cannot validate would itself be the fake-check class this suite exists to catch."
    },
    {
      "id": "gate-no-tests-caps-the-verdict",
      "kind": "document-only",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "**`--no-tests` caps the gate.** With hooks skipped the helper forces `waived`",
      "reason": "Added 2026-09-04 alongside the `converged` value, because documenting the cap for the first time made it a STATED gate this contract had to disposition. The enforcement is real and pre-existing: scripts/ship-commit.mjs sets `opts.gate = evidence.state === 'fresh' ? 'waived' : 'not-run'` BEFORE validation, printing a loud stderr line when that differs from what was asked, so a stronger verdict can never survive `--no-tests`. This is the one place the CLI rewrites a requested gate rather than refusing it, and it can only ever downgrade. It also satisfies ledger invariant REQ-behavioural-19096e7a, whose recorded gap was `untested` until tests/ship-commit-cli.test.mjs row 5d asserted BOTH halves (the capped trailer value and the `--no-verify` git argument). Document-only rather than executable for the same reason as the two gates above: the resulting exit code depends on the git + .audit/last-audit-run.json evidence state, so a hermetic cli-exit fixture cannot bind it deterministically, and the cli-exit scenario enum is closed with no member for this state."
    },
    {
      "id": "category-a-never-staged",
      "kind": "document-only",
      "reason": "'Do NOT git add <Category-A artifact>' (scripts/.sync-manifest.json, dashboard/index.html, dashboard/telemetry.html, docs/architecture-map.md) is an instruction to the AGENT running /ship about what NOT to stage. Still document-only as a GATE — there is no ship-CLI exit code to assert, because the rule constrains what the model chooses to stage, not what a command returns. REVISED 2026-07-26: the previous reason claimed this was 'enforced downstream by .gitignore, not by a ship CLI' and stopped there, which read as 'not mechanically checkable' and was wrong — the SKILL TEXT side is now guarded by tests/skill-staging-instructions.test.mjs, which fails when any shell-fenced `git add <literal path>` line in skills/** names a gitignored path. That guard exists because prose alone failed twice: dashboard/index.html (2026-06) got a 'do NOT stage' note, and docs/architecture-map.md then kept its `git add` line for six days sitting two steps above that very note until an agent followed it on 2026-07-26."
    },
    {
      "id": "step-0-5-gates-non-blocking",
      "kind": "document-only",
      "reason": "'Step 0.5 pre-ship gates are non-blocking by default; a query failure never makes the gate stricter OR blind' is a NEGATIVE guarantee about the agent's orchestration of the pre-ship queries. Asserting an absence-of-blocking across cloud-off / query-failure / no-session states is a judgement about agent flow, not a single CLI exit; document-only."
    },
    {
      "id": "unremediated-acceptances-never-blocks",
      "kind": "document-only",
      "stated": "never blocks, and there is no override flag for it",
      "reason": "Step 0.5e's nudge-not-gate guarantee. The READ half is mechanically honest and covered by the reader's own failure contract — getUnremediatedAcceptances returns [] on both cloud-off and query failure, exactly like getUnlockedFixes — so the query can never make the step stricter or blind. But 'never BLOCKS the ship' is a claim about what the agent does with a non-empty row set, and there is deliberately no override flag to assert against (the absence of one is the point: an override implies something to override). Same shape and same document-only rationale as step-0-5-gates-non-blocking above; asserting an absence-of-blocking is a judgement about agent flow, not a CLI exit code."
    },
    {
      "id": "unit-test-lock-refuses-unverifiable-claims",
      "kind": "document-only",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "the writer refuses a missing path or empty rationale",
      "reason": "Step 0.5b's unit-test lock path. The refusals are REAL and unit-tested (tests/unit-test-lock-kind.test.mjs pins all three: a non-existent test path, a path resolving outside the repo, and a missing --description), so this is not an unbacked claim. It is document-only because the refusal surfaces as a JSON `{ok:false, error}` payload rather than a process exit code — cross-skill.mjs's uniform contract is JSON-on-stdout with exit 0 — and `cli-exit` is the only CLI oracle available, against a CLOSED scenario registry. Claiming a cli-exit oracle here would assert an exit code the command deliberately does not produce, which is itself the fake-check class this suite exists to catch. Revisit if cross-skill ever adopts non-zero exits for refusals; the scenario would then be real."
    },
    {
      "id": "final-review-credit-advisory-exit-zero",
      "kind": "document-only",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "the reader always exits 0",
      "reason": "Step 6.7's nudge-not-gate guarantee for the final-review credit card. The READ half is mechanically honest and unit-covered: cmdFinalReviewPending maps every failure to one of a closed diagnostic enum and returns a discriminated {ready|disabled|unavailable} result rather than throwing, and renderFinalReviewCard returns '' for disabled, for a zero-count ready, and for any unrecognised shape (tests/final-review-card.test.mjs pins all three empty-output paths plus the no-leak case; tests/final-review-pending.test.mjs pins the classifier and counts). Document-only for the same reason as step-0-5-gates-non-blocking and unremediated-acceptances-never-blocks directly above: 'exits 0 and therefore cannot stop the ship' is ultimately a claim about what the AGENT does with the output, and cross-skill.mjs's uniform contract is JSON-on-stdout with exit 0 for every subcommand, so a cli-exit oracle would assert an exit code the command produces unconditionally — true but vacuous, which is the fake-check class this suite exists to catch."
    },
    {
      "id": "upstream-queue-never-blocks",
      "kind": "document-only",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "Never blocks, and there is no override flag",
      "reason": "Step 0.5h's nudge-not-gate guarantee for the upstream issue queue. The READ half is mechanically honest: `upstream list` is a cross-skill.mjs subcommand, so it returns {ok:true, cloud:false} on cloud-off and a JSON error payload on a store failure rather than throwing, and the step is instructed to skip silently in both cases -- a query failure can make it neither stricter nor blind. But 'never BLOCKS the ship' is a claim about what the AGENT does with a non-empty row set, and there is deliberately no override flag to assert against (the absence of one is the point: an override implies something to override). Same shape and same document-only rationale as step-0-5-gates-non-blocking and unremediated-acceptances-never-blocks above. The deeper reason it can only ever advise is the gate-level rule this repo already applies elsewhere: the queue is CLOUD state, and the commit being pushed cannot change it, so blocking on it would be the cried-wolf shape that earns --no-verify."
    },
    {
      "id": "persona-gate-unmeasured-is-not-silent",
      "kind": "document-only",
      "statedIn": "skills/ship/SKILL.md",
      "stated": "`measured: false` means the gate asked nothing",
      "reason": "Step 0.5a, added 2026-09-07 after a consumer measured the gate printing `gate silent` — the phrase for *no session exists* — while holding 2 open P0, because `--repo \"$PERSONA_TEST_REPO_NAME\"` was a shell expansion the host never populated. The DATA half is mechanically enforced and tested, and this reason names it so the claim is not unbacked: `explicit-preferred` resolves `--repo` -> PERSONA_TEST_REPO_NAME -> ambient git identity -> `{measured:false, reason}` (scripts/lib/cross-skill/scope.mjs), the handlers make NO store call against an unresolved scope, and tests/cross-skill-scope-resolver.test.mjs + tests/cross-skill-store-calls.test.mjs pin both directions — including that a NAMED repo never consults the ambient checkout and an unknown one is still UNKNOWN_REPO. Document-only for the same reason as unit-test-lock-refuses-unverifiable-claims and step-0-5-gates-non-blocking: what is being contracted HERE is which CARD /ship prints from that payload, cross-skill.mjs exits 0 for every subcommand by design, and `cli-exit` is the only oracle in a CLOSED scenario registry — asserting an exit code the command never produces would itself be the fake-check class this suite exists to catch."
    }
  ],
  "ignoredCandidates": [
    {
      "line": "> \"skills:hydrate\": \"node -e \\\"const{execFileSync}=require('node:child_process'),p=require('node:path'),f=require('node:fs');const main=p.dirname(execFileSync('git',['rev-parse','--path-format=absolute','--git-common-dir'],{encoding:'utf8'}).trim());const dir='scripts/.claude-skills';const src=p.join(main,dir);if(p.resolve(dir)===p.resolve(src)){console.log('[hydrate] main checkout - nothing to do');process.exit(0)}if(!f.existsSync(src)){console.error('[hydrate] no tooling at '+src+' - re-sync the main checkout first');process.exit(1)}f.cpSync(src,dir,{recursive:true});const prune=(s,d)=>{if(!f.existsSync(d))return;for(const e of f.readdirSync(d,{withFileTypes:true})){const sp=p.join(s,e.name),dp=p.join(d,e.name);if(e.isDirectory()){if(!f.existsSync(sp)){f.rmSync(dp,{recursive:true,force:true});continue}prune(sp,dp);if(f.readdirSync(dp).length===0)f.rmSync(dp,{recursive:true,force:true})}else if(!f.existsSync(sp))f.rmSync(dp,{force:true})}};prune(src,dir);const man='scripts/.sync-manifest.json',ms=p.join(main,man),ok=f.existsSync(ms);if(ok){f.copyFileSync(ms,man)}console.log('[hydrate] copied '+(ok?2:1)+'/2 items from '+main+(ok?'':' - but NOT '+man+' (absent there): this tree has no bundle stamp'))\\\"\"",
      "reason": "The worktree-preflight marker block's inlined `skills:hydrate` recipe. Every node one-liner carries `require` and `process.exit`, two ENFORCEMENT_VERBS, and no phrasing of a runnable recipe avoids them — the verbs are JavaScript here, not an enforcement claim about this skill. The recipe is inlined rather than cited because in an unadopted consumer both of the old pointers are dead at once (no `skills:hydrate` script yet, and `docs/runbooks/` is not synced), so a skill file is the only carrier that reaches the reader. Byte-identical across all 16 contracts by construction: it is generated from CONSUMER_HYDRATE_NPM_SCRIPT via MARKER_BLOCK, and `checkSkill` pins the block in every copy."
    },
    {
      "line": "gitignored, so `git worktree add` never populates it — while `.claude/` (this",
      "reason": "Phase 0a. States what GIT does with an ignored path when creating a linked worktree (it does not populate it) against what the consumer sync layout puts where — `scripts/.claude-skills/` is gitignored, `.claude/` is tracked. A statement of fact about two external behaviours that together produce the MODULE_NOT_FOUND this step exists to pre-empt, not a gate /ship enforces. The preflight it introduces is a pair of `git rev-parse` reads whose result the AGENT acts on; there is no /ship exit code to assert."
    },
    {
      "line": "**Never `cd` to the main checkout to run these.** `ship-commit.mjs` and",
      "reason": "Phase 0a operator guidance, and deliberately NOT contracted: the failure it warns about is invisible to the guard that looks closest to it. Running from the main checkout makes ship-commit and cross-skill read THAT tree's HEAD, branch and commit_sha — but `--expect-head`/`--expect-branch` are captured in the same wrong place, so guard-b-identity-is-a-precondition verifies the wrong tree against itself and passes. `context:check` has the same blind spot (it takes its repo root from cwd). Nothing mechanical distinguishes the two checkouts at that point, which is exactly why this is written as a prohibition to the agent rather than claimed as a gate."
    },
    {
      "line": "**Never `npm run sync -- --target-path <worktree>`.** It aborts on unowned",
      "reason": "Phase 0a operator guidance. Describes SYNC's behaviour, not /ship's: the ownership manifest is gitignored, so a fresh worktree reads as an unowned repo and the sync aborts on collisions — and `--adopt-orphans`, the flag that clears the abort, does so by overwriting tracked files. The prohibition is about which recovery an agent reaches for; /ship emits no exit code for a command it is telling you not to run."
    },
    {
      "line": "`post-commit-drift` it exits 1 and prints a recovery command: **do not push.**",
      "reason": "Post-commit verification, and deliberately NOT contracted as executable: it DETECTS drift rather than preventing it, and \"do not push\" is an instruction to the AGENT about what to do with exit 1 — there is no further exit code for a recipe to assert. The exit-1 path is covered by tests/ship-commit-worktree-identity.test.mjs; the prevention half is docs/plans/ship-commit-transaction.md."
    },
    {
      "line": "(and needs `cat-file -t`, not `-e`, to detect: `-e` exits 0 for a tree).",
      "reason": "Explains WHY the deleted-directory probe uses `cat-file -t` rather than `-e` (`-e` exits 0 for a tree as well as a blob). A measured statement about git behaviour (2026-08-11), not a gate this skill enforces."
    },
    {
      "line": "never named. Pass each file. A DELETED directory is refused for the same reason",
      "reason": "Elaboration of guard-a: the directory and deleted-directory refusals are the same declare-your-scope rule applied to a pathspec git would expand. Asserted in tests/ship-commit-worktree-identity.test.mjs — existing directory, deleted directory, and a deleted FILE still committing normally."
    },
    {
      "line": "index holds, so it requires you to declare what you are shipping. There is no",
      "reason": "Continuation of the same sentence as the line above; same gate, same disposition."
    },
    {
      "line": "refuses an unscoped commit outright: it cannot know whose staged entries the",
      "reason": "Restates guard-a-unscoped-commit-refused (bound executably above) — the WHY behind the refusal, not a second gate. The exit-2 behaviour is asserted by the ship-unscoped-index-refusal recipe."
    },
    {
      "line": "> `pre-bundle-evidence` and you must pass the flags explicitly — it can never",
      "reason": "Elaboration of the same gate for the pre-bundle-evidence path — a marker written before auditedBranch existed cannot half-match. Asserted in tests/gate-evidence-tree-identity.test.mjs (present / explicit-null / absent are three distinct states) and tests/worktree-identity.test.mjs."
    },
    {
      "line": "> A head with no ref disposition is `incomplete-expectation` → exit 2, never a",
      "reason": "Elaboration of guard-b-identity-is-a-precondition (bound executably above): it names the specific reason code an INCOMPLETE bundle produces. The refusal is asserted by the ship-identity-absent-refusal recipe and by tests/worktree-identity.test.mjs, which pins incomplete-expectation across every illegal flag combination."
    },
    {
      "line": "--skill ship --models <csv> --gate <value> \\",
      "reason": "A line INSIDE the fenced example invocation, not a claim of its own. The literal `--gate <value>` placeholder is what trips the verb scan; the gate it illustrates is guard-b-identity-is-a-precondition, bound EXECUTABLY above."
    },
    {
      "line": "what's about to ship. The map itself is **never committed** — see below.",
      "reason": "Restates the category-a-never-staged gate for docs/architecture-map.md; not a separate claim. Its skill-text side is mechanically guarded by tests/skill-staging-instructions.test.mjs."
    },
    {
      "line": "> **`docs/architecture-map.md` is Category A and is NEVER staged.** This step",
      "reason": "Same restatement as above, in the explanatory blockquote. The rule is category-a-never-staged; this line names the artifact it applies to."
    },
    {
      "line": "> survived two steps away from that note. `git add` on a gitignored path *fails*,",
      "reason": "Describes GIT's own behaviour (git refuses to stage an ignored path without -f), not a project gate. Included to explain why the removed instruction failed SILENTLY — it paired that failure with 2>/dev/null || true."
    },
    {
      "line": "> It fails the byte-identical Category B test three independent ways: the header",
      "reason": "Explains why docs/architecture-map.md is classified Category A (timestamp+sha+refresh_id header, LLM-written summaries, renders from the cloud symbol_index). A classification rationale, not an enforced gate — the enforcement is the .gitignore entry plus GENERATED_UNTRACKED_TARGETS in check-docs-refs.mjs."
    },
    {
      "line": "### 0.5e — Accepted findings that were never remediated",
      "reason": "Section heading. 'never remediated' names the POPULATION the step reads (findings whose remediation_state never advanced), not a guarantee the step enforces. The step's actual enforcement claim is contracted as unremediated-acceptances-never-blocks."
    },
    {
      "line": "<n> finding(s) you accepted were never marked fixed (showing <=5):",
      "reason": "Line inside the fenced OUTPUT TEMPLATE the agent prints — sample text shown to the user, not a rule the skill enforces. 'never marked fixed' restates the view's own predicate (remediation_state NULL/pending/planned)."
    },
    {
      "line": "plan was never amended), but say so rather than printing it as a code path.",
      "reason": "Interpretation guidance for plan-mode rows: it explains what a stale plan-mode finding MEANS (the plan was never amended) so the agent does not print a section reference as if it were a file path. Descriptive, not an enforced gate."
    },
    {
      "line": "the weekly GH workflow, never by /ship directly.",
      "reason": "Pre-existing prose about who updates the arch-drift sticky issue; it became a candidate only because the 2026-07-26 edit reflowed the paragraph and moved 'never' onto this line. Scope statement about a GH workflow, not a /ship gate."
    },
    {
      "line": "are \"not applicable\", **not** \"no obligations\". Report it as unmeasured; never",
      "reason": "An instruction to the AGENT about how to render an unmeasured result — the measured:false vs genuine-zero distinction. The producing side is mechanical and tested (tests/cross-skill-unlocked-scope.test.mjs asserts measured:false carries a reason and never widens to global), but \"how the agent phrases the card\" has no oracle."
    },
    {
      "line": "### 0.5g — Migration realization gate (ENFORCED by the binary, checked HERE too)",
      "reason": "Step 0.5g, the migration-realization gate. It is genuinely ENFORCED — scripts/ship-commit.mjs refuses to compose a commit and exits 2 with an `AGENT FIX:` line when the database is missing a bundled migration — but it stays uncontracted rather than bound to a wrong oracle: the cli-exit oracle drives a CLI against a FILESYSTEM fixture, and this gate's trigger is DATABASE state (a migration ledger missing a bundled filename), which no recipe shape can construct. Declaring cli-exit anyway would assert a binding that does not hold. The logic is covered by tests/schema-realization.test.mjs and tests/write-path-fail-closed.test.mjs, and the refusal is observed END-TO-END against a real database by tests/db-schema-realization-live.test.mjs (db:suites:gate + the postgres-parity CI job), which evicts a ledger row and asserts ERR_SCHEMA_BEHIND. Contracting it needs a DB-backed oracle — a separate change with no current requirement."
    },
    {
      "line": "**`unverified` must name a concrete blocked prerequisite** — \"no network in this",
      "reason": "Step 6.8 instruction about what the `unverified` terminal state must contain. Whether a stated prerequisite is 'concrete' is a judgement call; /ship emits no exit code for the quality of a status line it wrote into status.md."
    },
    {
      "line": "environment\", \"no consumer checkout on this machine\" — never a bare \"not",
      "reason": "Continuation of the same sentence — the prohibition on an undefined 'not applicable'. Same judgement, same absence of an oracle."
    },
    {
      "line": "excuse. **Never inherit the producer-side green.**",
      "reason": "The core Step 6.8 discipline, addressed to the agent. Asserting it would mean proving a consumer-side retrieval actually happened and was not skipped — exactly the thing the three terminal states exist to make an operator declare honestly, because no mechanical check can."
    },
    {
      "line": "Advisory by construction: the push already happened, so this cannot block it —",
      "reason": "A NEGATIVE guarantee about Step 6.8 (it advises, it never blocks) — the same shape and rationale as this contract's existing step-0-5-gates-non-blocking and unremediated-acceptances-never-blocks entries. Asserting an absence-of-blocking is a judgement about agent flow, not a CLI exit."
    },
    {
      "line": "and per the gate-level rule, machine and remote state may advise where repo",
      "reason": "A restatement of the repo-wide gate-level rule, cited to explain WHY Step 6.8 is advisory. The enforcement verb belongs to that rule as applied elsewhere; /ship asserts nothing new here."
    },
    {
      "line": "state may block. → `references/verification-discipline.md` §6.",
      "reason": "Tail of the same sentence plus the pointer to the shared reference. No claim of its own."
    },
    {
      "line": "| `references/verification-discipline.md` | Verification discipline — pinned citations, figure provenance, two-direction proof, attribution, consumer-side checks. | Step 6.8 — the push succeeded and the artifact must be verified from the consumer side. |",
      "reason": "A REFERENCE-TABLE row. The 'Read when' cell is a trigger description for opening a reference file, not a gate /ship enforces."
    },
    {
      "line": "Waiting is not a way to clear this gate. Read them:",
      "reason": "Rhetorical instruction to the operator about the lock nudge's intent; the OBLIGATIONS LOST block it sits in is explicitly labelled non-blocking and has no override flag, exactly like the REGRESSION LOCK GATE block above it."
    },
    {
      "line": "it survives a session boundary but never ships as-is.",
      "reason": "Step 2's pickup of a pending Step 6.8 verification note. Describes properties of the gitignored scratch file (`.claude/tmp/ship-verification-pending.md`) itself — it persists across a session boundary (unlike in-memory agent state) but is never staged/committed as-is (Step 2 reads and deletes it, folding its content into a proper status.md entry). Not an enforcement claim: nothing blocks on the file's presence or absence, and its own loss is explicitly framed two lines below as 'an acceptable loss for advisory documentation (never a gate)'."
    },
    {
      "line": "that provisions `scripts/.claude-skills/**` never merges npm scripts into a",
      "reason": "States a fact about the consumer-sync mechanism (added 2026-08-14 alongside `--if-present`): npm scripts are never merged into a consumer's package.json by the file-sync that provisions scripts/.claude-skills/**. Explains WHY `--if-present` is load-bearing; not itself a claim this skill enforces — the sync mechanism's own behaviour lives in scripts/sync-to-repos.mjs, outside /ship's exit code."
    },
    {
      "line": "on every such push (reported by a consumer 2026-08-14) — harmless (never",
      "reason": "Continuation of the --if-present rationale: describes the field-observed defect (an avoidable npm error) this fix removes, and confirms it never blocked the ship even before the fix — a NEGATIVE historical observation, not an enforcement claim. Nothing in /ship asserts an exit code over whether a prior bug was blocking; the fix is a UX improvement (silence a harmless error), not a new gate."
    },
    {
      "line": "**Write the outcome to `.claude/tmp/ship-verification-pending.md` — never by",
      "reason": "Step 6.8 instruction to the agent about WHERE to record the consumer-verification outcome (a gitignored scratch file, not the already-pushed status.md entry). Same shape as the existing Step 6.8 entries here ('unverified must name a concrete blocked prerequisite', 'Never inherit the producer-side green'): a discipline addressed to the agent, with no CLI exit code for a cli-exit recipe to assert — /ship writes nothing that checks whether the agent complied."
    },
    {
      "line": "> so the handoff silently never happens and the only symptom is a note that",
      "reason": "Step 6.8. Explains the FAILURE MODE that motivated resolving the main checkout (a deleted worktree destroys the note). A statement about what happens absent the fix, not a rule /ship enforces."
    },
    {
      "line": "primary GPT/Gemini-round finding was never surfaced here at all, however long",
      "reason": "Step 6.7 (docs/plans/skill-efficacy-census.md Phase 1, 2026-08-22). Narrates the PRE-FIX bug the widened-bucket change closes -- a primary finding could sit fixed with user_action still null and this step would never show it -- not a guarantee about current or future behaviour. Same shape as the 2026-08-14 migration-realization narrative entries above ('A consumer reported...', 'on every such push...'): a historical description of a defect, matched only because the verb scanner sees 'never'. The actual current-behaviour claim (final-review-pending now reads the merged pendingQueue) is mechanically covered by tests/final-review-card.test.mjs's negative-control assertions (a bucket:null item prints --bucket primary, never --bucket shadow-only)."
    },
    {
      "line": "node scripts/remediation-reconcile.mjs --apply --cap 5 2>/dev/null",
      "reason": "0.5e's auto-reconcile invocation line — a runnable recipe (`node`, `--apply`) the enforcement-verb scanner flags on the tool name alone, same class as the skills:hydrate one-liner above. The CLI's own flag/apply/dry-run behaviour is tested directly; this line is the command an operator runs, not a claim /ship enforces about itself."
    },
    {
      "line": "| `references/migration-credentials.md` | Which role applies migrations, why the runtime DSN cannot, and where its credential belongs (never in .env). | Step 0.5g — a store is behind and `--migrate` is refused with `42501` (`must be owner of table …` / `permission denied for schema public`). |",
      "reason": "The reference-index ROW for migration-credentials.md. Its Summary column must byte-match the file's `summary:` frontmatter (skills:check enforces that), so the enforcement verbs 'never'/'must'/'refused' are inherited from the reference's own one-line summary and cannot be reworded away independently. Index rows describe what a document covers; they are not gate claims about /ship's behaviour."
    },
    {
      "line": "> offering `/ship`, never by running it.",
      "reason": "Part of the explicit-invocation host-neutral rule header (cross-host-parity-v2, the disable-model-invocation body statement) — instructs the agent when to offer vs. run this skill. Descriptive guidance for the agent, not a mechanically enforced claim within this skill's own execution."
    },
    {
      "line": "> was never given — the skill would be citing its own execution as consent.",
      "reason": "Same explicit-invocation rule header — explains WHY self-invocation is wrong. Descriptive reasoning."
    },
    {
      "line": "Step 3's override handling. Never add one on the skill's own initiative to get",
      "reason": "Usage-table footnote instructing the agent never to synthesise an override flag on its own. Descriptive guidance for the agent following the skill, not code-level enforcement within this document."
    },
    {
      "line": "past a red gate — that is the gate working.",
      "reason": "Continuation of the same footnote."
    },
    {
      "line": "message naming this skill or its subject. Never inferred from surrounding",
      "reason": "The \"Where $ARGUMENTS comes from\" template line, synced verbatim from docs/audit/shared-references/input-acquisition.md into every skill that reads the token. It is authoring guidance for the agent reading the skill at parse time (where to look for input, in what priority) — no code in this repo exits or blocks on it; the enforcement, such as it is, is the human/agent following documented instructions, same class as every other prose \"never guess\" line already exempted in this contract."
    },
    {
      "line": "conversation. This site is `path+flags`; on empty input, ship without updating a plan — that is the documented default. Never adopt a plan path mentioned earlier in the conversation.",
      "reason": "A per-site \"_This site: …_\" annotation from the input-acquisition contract (Phase 5, cross-host-parity-v2), describing this ONE $ARGUMENTS occurrence's grammar class and empty-input behaviour for the agent parsing it. Descriptive guidance, not a mechanically enforced claim — no exit code or gate binds to it."
    },
    {
      "line": "`upstream:coverage:gate` rejects a non-uuid key. Full ids:",
      "reason": "Describes an external CLI's (upstream:coverage:gate, a different script) own validation behaviour — not a claim about this skill's execution."
    },
    {
      "line": "_This site: `path+flags` — **no flag means no override.** Each of these disables a gate, so they are read ONLY from the invocation — a conversation that merely mentions skipping tests must never become one._",
      "reason": "A per-site \"_This site: …_\" annotation from the input-acquisition contract (Phase 5, cross-host-parity-v2), describing this ONE $ARGUMENTS occurrence's grammar class and empty-input behaviour for the agent parsing it. Descriptive guidance, not a mechanically enforced claim — no exit code or gate binds to it."
    },
    {
      "line": "> exists here only, since the sync never adds npm scripts. A consumer runs the",
      "reason": "Descriptive design-rationale prose (the \"why\" behind a fix or contract), not a claim that this skill's own execution enforces anything. Enforcement-verb words appear because the prose explains a defect or a rule in English, the same class as the pre-existing ignoredCandidates entries in this file."
    },
    {
      "line": "opt-in and unwired: the builder syncs, but the sync never adds npm scripts, so",
      "reason": "Descriptive design-rationale prose (the \"why\" behind a fix or contract), not a claim that this skill's own execution enforces anything. Enforcement-verb words appear because the prose explains a defect or a rule in English, the same class as the pre-existing ignoredCandidates entries in this file."
    },
    {
      "line": "> **Continuing work in the same worktree after a squash merge: rebase, never",
      "reason": "Step 6.3's squash-merge continuation callout (added 2026-09-08 from storyline upstream report ef229b6d-96ed-4792-98b7-e90276da00cd), warning the AGENT to `git rebase origin/<base>` rather than `git merge origin/<base>` when a follow-up branch shares history with a squash-merged `/ship` PR, and that a `mergeable: CONFLICTING` PR can correlate with zero CI runs firing. This is git-workflow guidance the agent applies while choosing which command to run before pushing — there is no `/ship` CLI exit code to assert, because ship-commit.mjs never sees which git command the agent chose beforehand. Same shape as the pre-existing 'Never `cd` to the main checkout to run these' and 'Never `npm run sync -- --target-path <worktree>`' Phase-0a entries above: a prohibition/preference addressed to the agent, not a claim of mechanical enforcement."
    },
    {
      "line": "Never blocks the ship.",
      "reason": "States that Step 0.5d's dashboard rebuild is advisory-only — descriptive of that step's own instruction, same class as the pre-existing \"never blocks\" statements already exempted elsewhere in this contract."
    },
    {
      "line": "`git add` a gitignored path, which either fails or force-adds a Category-A",
      "reason": "Descriptive design-rationale prose (the \"why\" behind a fix or contract), not a claim that this skill's own execution enforces anything. Enforcement-verb words appear because the prose explains a defect or a rule in English, the same class as the pre-existing ignoredCandidates entries in this file."
    },
    {
      "line": "**Call the script by path, never `npm run security:refresh`.** The sync never",
      "reason": "Descriptive design-rationale prose (the \"why\" behind a fix or contract), not a claim that this skill's own execution enforces anything. Enforcement-verb words appear because the prose explains a defect or a rule in English, the same class as the pre-existing ignoredCandidates entries in this file."
    },
    {
      "line": "consumer reported — and, because that flag exits **0** having run nothing,",
      "reason": "Continuation of the --if-present incident note (fixed this session). Historical/descriptive."
    },
    {
      "line": "turned every consumer refresh into a silent no-op with a success exit code.",
      "reason": "Continuation of the same incident note."
    },
    {
      "line": "path makes the step actually run where it was always meant to. Do not \"fix\" a",
      "reason": "Continuation of the same incident note."
    },
    {
      "line": "> empty, not a broken one, and this step must not read an empty list as",
      "reason": "Part of the friction-closure hook-rule block (cross-host-parity-v2 Phase 4): instructs the agent not to misread a correct-but-empty result as evidence. Descriptive guidance, not code-level enforcement."
    },
    {
      "line": "`--gate` — four values. The first two are **verified** (the helper queries the",
      "reason": "Added 2026-09-04 with the `converged` value. This line ENUMERATES the grammar; it makes no enforcement claim of its own. Each value's enforcement is dispositioned by its own gate above — gate-passed-refused-without-evidence and gate-converged-requires-verified-divergence — and the enum itself is mechanically pinned by GATE_VALUES in scripts/lib/commit-trailers.mjs plus the byte-pinned rejection string in tests/commit-trailers.test.mjs."
    },
    {
      "line": "**fixed**, so the tree moved *because of the gate*. Not a bypass, and it must",
      "reason": "Defines what `converged` MEANS (the tree moved because findings were applied), and asserts how a reader should INTERPRET it — 'not a bypass'. That is semantics for a human, not a condition any exit code can assert; the mechanical half (fresh evidence + store verdict + tree divergence) is gate-converged-requires-verified-divergence."
    },
    {
      "line": "gate via `--ignore-p0`/`--no-tests`/etc., OR verification unavailable (cloud",
      "reason": "Describes the situations that PRODUCE `waived`. `waived` is by definition the declared, unverified disposition — there is no verification for a gate contract to bind, which is the whole point of the value. Pre-existing semantics, unchanged by the 2026-09-04 addition; the line is new only because the surrounding block was rewritten."
    },
    {
      "line": "> never `passed`.",
      "reason": "Line-wrap continuation of the preceding sentence ('A partial commit of an audited worktree differs from the audited tree, so it is `converged` territory, never `passed`'), whose enforcement is gate-converged-requires-verified-divergence. A fragment carrying one verb and no independent claim."
    },
    {
      "line": "> commit after an audited ship inherits the previous commit's marker and must",
      "reason": "Explains WHY `--no-run-id` exists: freshness is `evidenceMs > headCommitTs`, so a follow-up commit inherits the prior marker. The refusal it describes (fresh evidence makes `not-run` illegal) is enforced in validateTrailerInput and byte-pinned by tests/commit-trailers.test.mjs ('fresh evidence + --gate not-run is rejected with the pinned line'); this line is the rationale, not a second gate."
    },
    {
      "line": "> disclaim it: `--no-run-id --gate not-run`. It omits `AI-Run-ID` entirely. Use",
      "reason": "Documents the `--no-run-id` flag's effect and the honesty condition on using it ('only when the claim is true'). The mechanical half — the flag omits AI-Run-ID and forces not-run — is covered end-to-end by tests/ship-commit-cli.test.mjs row 5b; the 'use it only when true' half is a judgement about the operator's claim, which no exit code can assert."
    },
    {
      "line": "> Skipping hooks can never buy a stronger verdict.",
      "reason": "Second sentence of the `--no-tests` cap claim, whose enforcement is dispositioned by gate-no-tests-caps-the-verdict in gates[] above. Carries one verb ('never') and restates the preceding sentence's consequence rather than adding a condition."
    },
    {
      "line": "A queue it could not read renders **`unmeasured`**, never `0`. That distinction",
      "reason": "Step 2b. Describes what scripts/backlog-snapshot.mjs PRINTS, not something /ship enforces — the skill pastes a line the command produced. The claim is real and mechanically covered where it belongs, at the formatter: tests/backlog-snapshot.test.mjs asserts that an envelope reporting measured:false, cloud:false or a non-repo scope renders , and that no field renders 0 in that case. There is no /ship exit code to assert against, so a cli-exit oracle here would be a fake check."
    },
    {
      "line": "The command always exits 0; it is a nudge, never a gate.",
      "reason": "Step 2b. A NEGATIVE guarantee about an advisory command, the same shape and the same disposition as step-0-5-gates-non-blocking and final-review-credit-advisory-exit-zero above. backlog-snapshot.mjs exits 0 unconditionally — including from its own catch — so a cli-exit oracle would assert an exit code the command always produces: true but vacuous, which is the fake-check class this suite exists to catch. What is NOT vacuous, and is tested, is that an unreadable queue renders unmeasured rather than 0."
    },
    {
      "line": "entry you are already authoring. Never let a script write `status.md`: PR #87",
      "reason": "Step 2b. An instruction to the AGENT about how to place the backlog line, plus the incident that motivates it — not a gate /ship runs. The property it protects IS enforced, just elsewhere and by a different mechanism: scripts/backlog-snapshot.mjs writes no file at all (it prints one line to stdout), and status:integrity:gate fails the push if any session-log entry is lost or rewritten rather than appended to. So there is nothing for a cli-exit oracle to assert here; asserting one would be the fake-check class this suite catches. The PR #87 reference is the reason the instruction is phrased as a prohibition: a tool rewriting that file destroyed 19,257 lines and reached main through a full check run."
    },
    {
      "line": "clear must survive to the next drain, which is why `clear` refuses to delete",
      "reason": "Step 2 prose explaining why `pending-note clear` takes explicit names. `must` and `refuses` describe the CLI's argument handling — a fact about lib/worktree-preflight.mjs, tested in tests/upstream-triage-2026-09-07.test.mjs — not an enforcement claim /ship makes about itself."
    },
    {
      "line": "session boundary but never ship as-is.",
      "reason": "Restates that `.claude/tmp/` is gitignored scratch. `never ship` is a description of the directory, not a gate. Reworded from the pre-2026-09-07 singular phrasing when Step 2 moved to draining N notes."
    },
    {
      "line": "| the synced consumer bundle | **authoritative**: `node scripts/.claude-skills/lib/sync-isolation-verify.mjs`, run *in the consumer's MAIN checkout* — note the `lib/` segment, it is a module rather than a top-level script. A linked worktree cannot answer this, and since 2026-09-07 it SAYS so: `runGates` refuses a proven linked worktree at `preflight` with exit 2. The manifest records what the last sync wrote to the MAIN checkout's disk, and `skills:hydrate` populates a worktree by copying those same files — so a run there would re-read bytes hydrate just copied and report agreement it manufactured. Hydrate now carries the manifest as well as the tooling tree (upstream 5bc7ff30), so that refusal is explicit rather than the accident of a missing file. `npm run sync:dry` from here is the pre-check, not the verdict | zero unexpected diffs; no orphans |",
      "reason": "Step 6.8's retrieval table row. Names sync-isolation-verify's own `preflight` refusal of a linked worktree — an enforcement claim about THAT module (tested in tests/upstream-triage-2026-09-07.test.mjs), reported to the reader so they run it in the right tree. /ship enforces nothing here; the whole step is advisory by construction."
    },
    {
      "line": "**Write the outcome with `pending-note write` (body on stdin) — never by",
      "reason": "The instruction that the note is written by the CLI rather than by re-opening a pushed status.md entry. `never` bars an ACTION the agent might take, which is what a skill step is for; it asserts no gate. Reworded 2026-09-07 when the fixed filename became one file per ship."
    },
    {
      "line": "simply sit there unread — an acceptable loss for advisory documentation (never a",
      "reason": "Says explicitly that an undrained note is tolerated and that this step is NOT a gate — the opposite of an enforcement claim. Rewrapped 2026-09-07; the pre-existing disposition covered the same sentence on one line."
    },
    {
      "line": "gate), not a reason to force a push now.",
      "reason": "Continuation of the line above — the word `gate` appears only inside \"(never a gate)\". Rewrapped 2026-09-07."
    },
    {
      "line": "> never appears. `.claude/tmp/` resolves per-tree, so a hand-written path means",
      "reason": "The 2026-08-14 incident callout: a note written in a deleted worktree is lost silently. Describes a past failure, claims no enforcement. Reworded 2026-09-07 because both steps now resolve the path in code rather than quoting a recipe."
    },
    {
      "line": "naming the resolution it attempted, and NEVER as \"gate silent\":",
      "reason": "The second half of the claim contracted at persona-gate-unmeasured-is-not-silent, split across two lines by the 80-column wrap. `stated` is a single-line span, so the continuation needs its own disposition; the claim, its enforcing code and its tests are named in that gate's reason."
    },
    {
      "line": "⚠ UX GATE — UNMEASURED (<reason>)",
      "reason": "The literal first line of the card /ship prints. `gate` is the card's own title text, not an enforcement claim about the card. The behaviour the card reports is contracted at persona-gate-unmeasured-is-not-silent."
    },
    {
      "line": "**Closed failure semantics — never a NEW blocker**:",
      "reason": "Pre-existing Step 0.5a heading, re-emitted as a changed line because the block around it was rewritten. It is the exact claim step-0-5-gates-non-blocking already dispositions as document-only: asserting an absence-of-blocking across cloud-off / unmeasured / query-failure / no-session states is a judgement about agent flow, not a single CLI exit."
    },
    {
      "line": "node scripts/ux-lock-run.mjs spec --specs 'tests/e2e/*.spec.js' --commit \"$(git rev-parse HEAD)\" --run-context ship-gate",
      "reason": "Command-example text touching a technical identifier (\"require(\", \"-gate\"/\"GATE=\" as a flag/value name) that the enforcement-verb scanner matches as a whole word; not an enforcement claim about this skill's own behaviour. Introduced by the 2026-09-10 PowerShell-portability pass (cross-host-parity follow-up)."
    },
    {
      "line": "GATE=not-run                     # passed | converged | waived | not-run — see AI-Gate above",
      "reason": "Command-example text touching a technical identifier (\"require(\", \"-gate\"/\"GATE=\" as a flag/value name) that the enforcement-verb scanner matches as a whole word; not an enforcement claim about this skill's own behaviour. Introduced by the 2026-09-10 PowerShell-portability pass (cross-host-parity follow-up)."
    },
    {
      "line": "node scripts/ship-commit.mjs --message-file \".claude/tmp/ship-commit-msg-$EPOCH.txt\" --skill ship --models \"$MODELS\" --gate \"$GATE\" --expect-head \"$SHIP_HEAD\" --expect-branch \"$SHIP_BRANCH\" --path scripts/lib/*.mjs --path tests/*.test.mjs",
      "reason": "Command-example text touching a technical identifier (\"require(\", \"-gate\"/\"GATE=\" as a flag/value name) that the enforcement-verb scanner matches as a whole word; not an enforcement claim about this skill's own behaviour. Introduced by the 2026-09-10 PowerShell-portability pass (cross-host-parity follow-up)."
    },
    {
      "line": "node scripts/ship-commit.mjs --message-file \".claude/tmp/ship-commit-msg-$EPOCH.txt\" --skill ship --models \"$MODELS\" --gate \"$GATE\" --expect-head \"$SHIP_HEAD\" --expect-branch \"$SHIP_BRANCH\" --path scripts/foo.mjs --path tests/foo.test.mjs",
      "reason": "Command-example text touching a technical identifier (\"require(\", \"-gate\"/\"GATE=\" as a flag/value name) that the enforcement-verb scanner matches as a whole word; not an enforcement claim about this skill's own behaviour. Introduced by the 2026-09-10 PowerShell-portability pass (cross-host-parity follow-up)."
    },
    {
      "line": "**This step is ALWAYS advisory — it never blocks a ship.** Verified against",
      "reason": "Upstream report cbd8b539 (wine-cellar-app) fix, reconciled with a concurrent session's independent fix to the same paragraph (83467058): Step 0.5c's failure-matrix intro. Restates the pre-existing step-0-5-gates-non-blocking disposition ('Step 0.5 pre-ship gates are non-blocking by default') for this specific step — a negative guarantee about agent orchestration, not a CLI exit code. 'Verified against' introduces the correction itself (the SUPABASE_AUDIT_URL/SERVICE_ROLE_REQUIRED/RPC vocabulary was stale relative to the postgres-parity migration), which is a documentation accuracy claim, not an enforcement claim."
    },
    {
      "line": "Non-JS/TS stack → skips silently (`reason: 'unsupported-stack'`), exit 0,",
      "reason": "Describes scripts/symbol-index/refresh.mjs's own early-return behaviour (verified against the source at fix time) for a non-JS/TS repo — a property of that CLI, not a claim /ship's own execution enforces. Same disposition class as the pre-existing 'exits 0'/'always exits 0' entries elsewhere in this file describing another script's contract."
    },
    {
      "line": "or refresh-lock conflict) → `refresh.mjs` exits 1 or 2 and writes a message",
      "reason": "Describes scripts/symbol-index/refresh.mjs's own error-exit behaviour (RepoRegistrationError/RefreshInFlightError/LockAbortError -> exit 1 or 2, verified against refresh-errors.mjs) — a property of that CLI, not a claim about /ship's own execution. The exit code belongs to a script this step invokes with `|| true`, so it is swallowed before /ship sees it."
    },
    {
      "line": "to stderr; this step's own `|| true` swallows the non-zero exit so the ship",
      "reason": "Continuation of the same sentence describing refresh.mjs's exit behaviour and this step's own `|| true` swallow — a design fact about how a non-blocking advisory step is composed (shell-level, not a /ship CLI branch), not a new enforcement claim."
    },
    {
      "line": "is never blocked, but the stderr line still prints — read it if the local",
      "reason": "Tail of the same sentence spanning the three entries above it: confirms this step remains non-blocking (restates step-0-5-gates-non-blocking) even on a swallowed non-zero exit, and that the printed stderr line is the operator's only signal. Not a new claim — the same design fact continued across a line wrap."
    },
    {
      "line": "JSON form pages by a **keyset cursor**, never an offset — the queue is drained",
      "reason": "Descriptive prose about the final-review-pending CLI's paging contract (keyset cursor, --page-size alias, cap 200), which is enforced by resolveNudgePage + decodeQueueCursor and pinned by tests/final-review-pending.test.mjs and tests/final-review-pending-db.test.mjs — not a claim that this skill's own execution enforces anything. Same class as the existing design-rationale entries."
    },
    {
      "line": "adjudication (measured before the change: a 50-row cap with no way past it",
      "reason": "Descriptive prose about the final-review-pending CLI's paging contract (keyset cursor, --page-size alias, cap 200), which is enforced by resolveNudgePage + decodeQueueCursor and pinned by tests/final-review-pending.test.mjs and tests/final-review-pending-db.test.mjs — not a claim that this skill's own execution enforces anything. Same class as the existing design-rationale entries."
    },
    {
      "line": "`--page-size` is the alias of `--limit` (default 20, cap 200 — the same",
      "reason": "Descriptive prose about the final-review-pending CLI's paging contract (keyset cursor, --page-size alias, cap 200), which is enforced by resolveNudgePage + decodeQueueCursor and pinned by tests/final-review-pending.test.mjs and tests/final-review-pending-db.test.mjs — not a claim that this skill's own execution enforces anything. Same class as the existing design-rationale entries."
    },
    {
      "line": "here is never undone by a later triage pass. The card now surfaces a",
      "reason": "Descriptive prose about the credit-card's two-axis labelling and the replay-preserves-rulings behavior (recordFinalReviewFindings upsert+prune), enforced by scripts/lib/store/runs-findings.mjs and pinned by tests/final-review-replay-db.test.mjs and tests/final-review-persistence-isolation.test.mjs -- not a claim that this skill's own execution enforces anything. Same class as the existing design-rationale entries."
    },
    {
      "line": "consolidated union-diff gate) silently wiped every `user_action` /",
      "reason": "Descriptive prose about the credit-card's two-axis labelling and the replay-preserves-rulings behavior (recordFinalReviewFindings upsert+prune), enforced by scripts/lib/store/runs-findings.mjs and pinned by tests/final-review-replay-db.test.mjs and tests/final-review-persistence-isolation.test.mjs -- not a claim that this skill's own execution enforces anything. Same class as the existing design-rationale entries."
    },
    {
      "line": "temp files there, but it sits outside the repo and `--message-file` refuses",
      "reason": "Documents a PRE-EXISTING, already-enforced refusal — checkMessageFileSafety's escapes-repo check and messageFileError's AGENT FIX text, both already tested in tests/commit-trailers.test.mjs (the escapes-repo/sensitive/unresolvable cases, lines ~376-392) before this change. This SKILL.md callout (upstream report 1c792b2e) is the first time that behaviour is stated in prose here; it names an existing mechanism proactively, it does not introduce a new one. A formal cli-exit binding for the refusal itself is a separate, future addition, not required by this doc-only fix."
    },
    {
      "line": "**Never pass `--repo \"$PERSONA_TEST_REPO_NAME\"` as a shell expansion** — see",
      "reason": "Step 0.5a operator instruction about how to INVOKE the command, not a gate /ship enforces (condensed 2026 Phase-1 size-reduction; supersedes the removed longer paragraph with the same disposition). `never` forbids a shell form: a Claude Code session inherits neither the consumer's .env nor ~/.audit-loop.env, so the expansion yields an empty flag. Nothing in this repo can assert what an agent types into a shell; the defence that IS mechanical is the CLI's own resolution chain, contracted at persona-gate-unmeasured-is-not-silent."
    },
    {
      "line": "`references/pre-ship-gate-queries.md` §0.5a for why.",
      "reason": "Cites `references/pre-ship-gate-queries.md` by path; the enforcement-verb scanner matches on the substring \"gate\" inside that FILENAME, not an enforcement claim. Same class as the pre-existing 'command-example text touching a technical identifier' entries in this file."
    },
    {
      "line": "`sessionId: null` **with `measured: true`** → the gate is genuinely silent —",
      "reason": "Names the ONE state in which \"gate silent\" is an honest report — the repo resolved and has no persona session. An instruction about which of two cards the agent prints, under step-0-5-gates-non-blocking's document-only disposition; the `measured` field it branches on is the tested half named at persona-gate-unmeasured-is-not-silent. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry for this line.)"
    },
    {
      "line": "Not an obligation — a repo that has never locked anything reports `agedOut: 0`.",
      "reason": "Describes a REPORTING outcome for a repo with no locking history, not a gate. The counter is advisory in both directions: 0 is not a pass and non-zero is not a block. (Condensed 2026 Phase-1; same disposition as the pre-existing 0.5b prePractice-reporting entries this replaced.)"
    },
    {
      "line": "**Never hand-derive a repo id** — pass no flag (ambient identity) or",
      "reason": "Operator guidance whose FAILURE MODE is mechanically caught, so it is not a separate gate. resolveShipNudgeScope in scripts/cross-skill.mjs verifies --repo-id against listRepoIds(), translates a repo_uuid, and returns reason:unknown-repo-id + measured:false otherwise — so a hand-derived id can no longer produce an authoritative zero. tests/cross-skill-unlocked-scope.test.mjs pins all three branches. Whether an operator CHOOSES to hand-derive one is advice, not an oracle. (Condensed 2026 Phase-1; same disposition as the pre-existing 'Never hand-derive a repo id to pass here' entry it replaced.)"
    },
    {
      "line": "`unlocked_fixes` and gets raised again. The command refuses a missing target",
      "reason": "Describes what `repoint-regression-spec` REFUSES (a missing target path, an unresolvable repo, an ambiguous lock) — enforcement claims about that CLI, unit-tested in tests/dangling-regression-lock.test.mjs, not a claim /ship enforces about itself. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing 429683ac entries.)"
    },
    {
      "line": "history + why each of these rules exists: `references/pre-ship-gate-queries.md`",
      "reason": "Cites `references/pre-ship-gate-queries.md` by path; the enforcement-verb scanner matches on the substring \"gate\" inside that FILENAME, not an enforcement claim. Same class as the pre-existing 'command-example text touching a technical identifier' entries in this file."
    },
    {
      "line": "mixed `agedOut` total (a plan row can never be locked, so it is never a lost",
      "reason": "Step 0.5b's lead-with-agedOutByMode.code rule (condensed 2026 Phase-1). Explains WHY the banner leads with the code split rather than the mixed `agedOut` total — a plan row's primary_file is a section reference and no lock can ever cite it, so it is never a lost obligation. Descriptive rationale for a printed nudge's wording, same document-only class as step-0-5-gates-non-blocking; the mechanical half (agedOutByMode splitting on primary_file's shape) lives in the reader and is covered by tests/cross-skill-unlocked-scope.test.mjs."
    },
    {
      "line": "**First, a capped auto-reconcile pass** (best-effort, never blocks, no",
      "reason": "0.5e's auto-reconcile sub-step (docs/plans/remediation-state-verification-reconciler.md), re-wrapped by the Phase-1 condensation. Same disposition as the pre-existing entry it replaces: 'never blocks, no override flag' is a claim about what the AGENT does after the capped reconciler call returns, not a /ship CLI exit code — the reconciler itself is scripts/remediation-reconcile.mjs, whose own dry-run/apply/kill-switch contract is tested in tests/remediation-verification-selection.test.mjs and tests/mark-findings-remediation.test.mjs."
    },
    {
      "line": "bounds will never revisit on their own:",
      "reason": "Continuation of the auto-reconcile sentence above, describing WHY the pass exists (the live-audit lifecycle's session/round/14-day bounds will never re-check a surviving row). Design rationale, not an enforcement claim; same disposition as the auto-reconcile entry immediately above."
    },
    {
      "line": "mechanical-resolution path only, never a failure.",
      "reason": "Describes scripts/remediation-reconcile.mjs's own graceful-degradation behaviour (no Claude credential -> mechanical-only, never a crash) — a property of that CLI, asserted by its own tests, not a /ship gate."
    },
    {
      "line": "**Never add this to `agedOut`, and never report it as a backlog** — mention",
      "reason": "An instruction to the AGENT about how to READ two counters, not a gate the skill enforces. notYetDue counts rows under the 7-day maturity floor, which surface on their own; the imperative exists because both it and agedOut present as 'not shown' and folding them is the specific misreading this view invites. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "real leak: never shown again.",
      "reason": "Describes the CONSEQUENCE of a row crossing the 30-day ceiling unremediated — it stops appearing in this step's read. Not an enforcement claim: the ACCEPTANCES LOST block is explicitly non-blocking with no override flag. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "**Use `byMode.total` as `unremediated_count` — NEVER `rows.length`** (capped",
      "reason": "Instruction to the reading agent about which emitted field to use, not a claim that /ship blocks on anything. It IS mechanically guarded: tests/cross-skill-unlocked-scope.test.mjs asserts every capped nudge reader has a count* sibling and that cross-skill.mjs calls the counter wherever it calls the reader. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "If `> 0`, print — never blocks, no override flag. **Show the first 5 rows as",
      "reason": "Step 0.5e's nudge-not-gate guarantee (contracted at unremediated-acceptances-never-blocks) plus the 'show the first 5 rows as returned' display instruction, merged onto one line by the Phase-1 condensation. 'Show the first 5' caps output verbosity for the agent, not behaviour — the reader's own page/order is the only mechanical bound (asserting how many rows a model chooses to print is not a gate)."
    },
    {
      "line": "cannot see is indistinguishable from a leak. `open === total` always — one",
      "reason": "States an arithmetic identity of the emitted payload plus the reporting rationale for byDisposition.acceptedPermanent. `open === total` IS mechanically asserted — tests/unremediated-acceptance-disposition.test.mjs pins it against a seeded fixture matrix — which is why it is dispositioned here rather than as document-only. Nothing about it blocks a ship; 0.5e remains non-blocking per the unremediated-acceptances-never-blocks gate. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "`references/pre-ship-gate-queries.md` §0.5e.",
      "reason": "Cites `references/pre-ship-gate-queries.md` by path; the enforcement-verb scanner matches on the substring \"gate\" inside that FILENAME, not an enforcement claim. Same class as the pre-existing 'command-example text touching a technical identifier' entries in this file."
    },
    {
      "line": "check again inside the commit path and exits 2 there regardless of whether",
      "reason": "Step 0.5g's migration-realization gate. Genuinely enforced in scripts/ship-commit.mjs (exit 2 at Step 6.3), but deliberately left UNCONTRACTED rather than bound to a wrong oracle: the cli-exit oracle drives a CLI against a FILESYSTEM fixture, and this gate's trigger is DATABASE state (a migration ledger missing a bundled filename), which no recipe shape can construct. Covered by tests/schema-realization.test.mjs and tests/write-path-fail-closed.test.mjs. (Re-wrapped by the Phase-1 condensation; same disposition as the pre-existing 0.5g entries it replaces.)"
    },
    {
      "line": "this preflight ran. This early run is advisory, not a substitute gate.",
      "reason": "Step 0.5g's early advisory preflight (`ship-commit.mjs --check-migrations`). 'A SKILL step is an instruction to an agent and cannot block on its own' is the explicit disclaimer this text makes, not a claim that the preflight itself enforces anything — the commit-time enforcement stays uncontracted for the DB-state reason given above. (Re-wrapped by the Phase-1 condensation; same disposition as the pre-existing 0.5g preflight entries.)"
    },
    {
      "line": "**Cloud off / unreachable / no ledger ⇒ silently skipped**, never a block.",
      "reason": "Step 0.5g. Genuinely ENFORCED but deliberately uncontracted: the cli-exit oracle drives a CLI against a filesystem fixture, and this gate's trigger is DATABASE state, which no recipe shape can construct. Covered by tests/schema-realization.test.mjs and tests/write-path-fail-closed.test.mjs. (Re-wrapped by the Phase-1 condensation; same disposition as the pre-existing entry for this sentence.)"
    },
    {
      "line": "**On a block**: run `node scripts/setup-postgres.mjs --migrate`, then",
      "reason": "Step 0.5g's remedy instruction on a migration-realization block. Same DB-state uncontracted disposition as the sibling 0.5g entries above. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "Print its stdout verbatim — it renders the finished card. **Never blocks, no",
      "reason": "0.5g's cross-store drift sub-step (`npm run stores:drift`). 'Never blocks, no override flag' describes what the AGENT does with the output, not a /ship CLI exit code — store-drift.mjs's own always-exit-0 contract is asserted in tests/store-drift.test.mjs. (Re-wrapped by the Phase-1 condensation; same disposition as the pre-existing entry.)"
    },
    {
      "line": "nobody could reach is reported explicitly, never counted as current; if NO",
      "reason": "0.5g's drift-reader reporting rule: a store nobody could reach is named explicitly rather than counted as current. An instruction to the agent about how to render an unqueried result, same document-only class as step-0-5-gates-non-blocking; the producing side is store-drift.mjs's own contract, tested in tests/store-drift.test.mjs."
    },
    {
      "line": "secret store, never a file): `references/migration-credentials.md`. Incident",
      "reason": "Pointer to the reference carrying the credential-handling procedure; 'never a file' is guidance about where an operator keeps a secret, not a gate /ship enforces. (Re-wrapped by the Phase-1 condensation; same disposition as the pre-existing entry for this sentence.)"
    },
    {
      "line": "history behind the rules above: `references/pre-ship-gate-queries.md` §0.5g.",
      "reason": "Cites `references/pre-ship-gate-queries.md` by path; the enforcement-verb scanner matches on the substring \"gate\" inside that FILENAME, not an enforcement claim. Same class as the pre-existing 'command-example text touching a technical identifier' entries in this file."
    },
    {
      "line": "registered consumer's own store** (never a single ambient one — consumers are",
      "reason": "0.5h. Restates that `upstream-queues.mjs` reads every registered consumer's own store rather than the ambient one — a property of that CLI's resolution logic, tested in tests/upstream-store-discovery.test.mjs, not a claim /ship enforces about its own execution. (Condensed 2026 Phase-1; same disposition as the pre-existing 'Read EVERY consumer's store' entries it replaces.)"
    },
    {
      "line": "never a DSN or hostname.",
      "reason": "0.5h. A disclosure property of `upstream-queues.mjs`'s rendering (fingerprint plus consumer names only), asserted in tests/upstream-store-discovery.test.mjs — not a gate /ship enforces. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "not repo state; it always exits 0. **Read the `unqueried` / `no store` lines,",
      "reason": "0.5h. States that `upstream-queues.mjs` is advisory — it exits 0 whatever the queues hold, a property of the invoked CLI asserted in its own module, not a gate /ship itself runs — plus the instruction to read the unqueried/no-store lines rather than treat the count as complete. (Re-wrapped by the Phase-1 condensation; same disposition as the pre-existing entries it replaces.)"
    },
    {
      "line": "disposition ledger records what you typed, and `upstream:coverage:gate`",
      "reason": "0.5h. Names a constraint ANOTHER gate imposes (closing a report by uuid prefix fails `upstream:coverage:gate`) so the reader does not leave `npm run check` red. The enforcement is `upstreamTransition`'s boundary check plus check-upstream-probe-coverage.mjs's uuid shape rule, bound in tests/upstream-disposition-ratchet.test.mjs. /ship neither performs nor can assert it. (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "`references/pre-ship-gate-queries.md` §0.5h.",
      "reason": "Cites `references/pre-ship-gate-queries.md` by path; the enforcement-verb scanner matches on the substring \"gate\" inside that FILENAME, not an enforcement claim. Same class as the pre-existing 'command-example text touching a technical identifier' entries in this file."
    },
    {
      "line": "**Never blocks, no override flag** — collection state is cloud state the",
      "reason": "Step 0.5i (2026-08-23), re-wrapped by the Phase-1 condensation. Same nudge-not-gate guarantee and disposition as the pre-existing 0.5i entry it replaces: 'never blocks' is a claim about what the AGENT does with a non-empty result, and there is deliberately no override flag to assert against. `campaign.mjs stale` returns 0 on every path, so a query failure can make this step neither stricter nor blind."
    },
    {
      "line": "unreachable, or a campaign has never collected at all (never-started is not",
      "reason": "Step 0.5i. Describes the verb's SILENCE conditions — matched only because the verb scanner sees 'never'. Each named condition is a real branch in verbStale, and the never-started case is load-bearing (a campaign with no arm runs must read as 'nothing to say', not infinite staleness). (Re-wrapped by the Phase-1 condensation; same claim as the pre-existing entry.)"
    },
    {
      "line": "Incident history: `references/pre-ship-gate-queries.md` §0.5i.",
      "reason": "Cites `references/pre-ship-gate-queries.md` by path; the enforcement-verb scanner matches on the substring \"gate\" inside that FILENAME, not an enforcement claim. Same class as the pre-existing 'command-example text touching a technical identifier' entries in this file."
    },
    {
      "line": "| `references/pre-ship-gate-queries.md` | Step 0.5's pre-ship nudges (0.5a/b/e/g/h/i) — the incident history behind each rule, kept out of the routine flow. | Debugging a Step 0.5 nudge's behaviour, or before changing one. |",
      "reason": "The reference-index ROW for the new pre-ship-gate-queries.md file, added by the Phase-1 size-reduction. The enforcement-verb scanner matches on the substring \"gate\" inside the filename in column 1, same as every citation of this file elsewhere in this table. Index rows describe what a document covers; they are not gate claims about /ship's behaviour."
    },
    {
      "line": "**`docs/architecture-map.md` is Category A and is NEVER staged** — it embeds a",
      "reason": "0.5c, condensed 2026 Phase-2 size-reduction. States the Category-A classification (timestamp+sha header, LLM-written summaries, renders from cloud symbol_index) — a design-rationale fact about the file, not a gate /ship's own exit code enforces. The SKILL-TEXT side (no `git add` of this path) is guarded by tests/skill-staging-instructions.test.mjs, contracted at category-a-never-staged above; this line is the classification rationale, same disposition as the pre-existing 'Category A and is NEVER staged' entry it replaces."
    },
    {
      "line": "(the sync never adds npm scripts), so a consumer runs it by path and gets",
      "reason": "0.5d, condensed 2026 Phase-2. Describes the consumer-sync mechanism (npm scripts are never merged into a consumer's package.json) — a fact about scripts/sync-to-repos.mjs, not a claim this skill enforces about its own execution. Same disposition as the pre-existing 'opt-in and unwired: the builder syncs, but the sync never adds npm scripts' entry it replaces."
    },
    {
      "line": "Run it WITHOUT `|| true` — the **exit code is the signal** and must be read,",
      "reason": "0.5d. Operator instruction about how to invoke `build-dashboard.mjs reference` so its exit code reaches the agent — not a /ship CLI exit code itself; build-dashboard.mjs's own exit-non-zero-on-degraded-build contract is a property of that script. No new claim, pre-existing pattern (advisory step whose exit code the agent must read, not mask)."
    },
    {
      "line": "not masked. A non-zero exit must not abort the ship (this step is advisory):",
      "reason": "Continuation of the line above: the negative guarantee that a non-zero exit here must not abort /ship. Same document-only class as step-0-5-gates-non-blocking — a judgement about agent flow around an advisory step's exit code, not a /ship exit code of its own."
    },
    {
      "line": "exit code is a **reporting** signal, not a staging one:",
      "reason": "0.5d. States that build-dashboard.mjs's exit code is read for reporting only, never for staging — design rationale for how this advisory step is composed, not an enforcement claim about /ship's own execution."
    },
    {
      "line": "**Call the script by path, never `npm run security:refresh`** — that alias",
      "reason": "Step 6.5 operator instruction about how to INVOKE the refresher script, not a gate /ship enforces (condensed 2026 Phase-3; supersedes the removed longer paragraph with the same disposition). `never` forbids an alias that only exists in the source repo. Nothing in this repo can assert what an agent types into a shell."
    },
    {
      "line": "exists in the source repo only (the sync never merges npm scripts into a",
      "reason": "Continuation of the line above, naming WHY the npm alias is source-repo-only (the sync never merges npm scripts into a consumer's package.json) — a fact about the sync mechanism, not a /ship enforcement claim. Same disposition as the pre-existing entries for this incident."
    },
    {
      "line": "> empty. That is a correct empty, not a broken one: this step must not read an",
      "reason": "Step 6.6, condensed 2026 Phase-3. Instructs the agent not to misread a genuinely-empty session-review result as evidence that no friction existed — descriptive guidance about interpreting a query result, not a mechanical gate. Same disposition as the pre-existing entry for this sentence."
    },
    {
      "line": "**keyset cursor**, never an offset — the queue is drained by the very",
      "reason": "Step 6.7, condensed 2026 Phase-3. Describes final-review-pending's own paging contract (keyset cursor, never an offset), enforced by resolveNudgePage + decodeQueueCursor and pinned by tests/final-review-pending.test.mjs and tests/final-review-pending-db.test.mjs — not a claim this skill's own execution enforces. Same disposition as the pre-existing 'JSON form pages by a keyset cursor' entry it replaces."
    },
    {
      "line": "`--page-size` is the alias of `--limit` (default 20, cap 200); `--offset` is",
      "reason": "Step 6.7. Descriptive prose about the final-review-pending CLI's flag contract (page-size alias, cap 200, no offset support) — enforced by that CLI and its own tests, not a /ship gate. Same disposition as the pre-existing '--page-size is the alias of --limit' entry it replaces."
    },
    {
      "line": "outright, so recording a ruling here is never undone by a later triage pass.",
      "reason": "Step 6.7. Describes user_action's durable-override semantics (once set, it wins outright over a later triage pass) — a property of the store's write contract, not a /ship exit code. Same disposition as the pre-existing entry for this claim."
    },
    {
      "line": "round or a consolidated union-diff gate upserts the new snapshot and prunes",
      "reason": "Step 6.7, condensed 2026 Phase-3. Describes recordFinalReviewFindings's upsert-and-prune behaviour (a re-run does not erase prior rulings) — enforced by scripts/lib/store/runs-findings.mjs and pinned by tests/final-review-replay-db.test.mjs and tests/final-review-persistence-isolation.test.mjs, not a claim /ship's own execution enforces. Same disposition as the pre-existing entries for this incident."
    },
    {
      "line": "(never a gate), not a reason to force a push now. Incident history behind",
      "reason": "Step 6.8, condensed 2026 Phase-3. States explicitly that an undrained pending-note is tolerated and this step is NOT a gate — the opposite of an enforcement claim, plus a pointer to the new reference file (the enforcement-verb scanner matches on 'gate' inside the word '(never a gate)' and inside the word 'never'). Same disposition as the pre-existing 'never a gate), not a reason to force a push now' entry it replaces."
    },
    {
      "line": "path>`, deleted once the commit lands) works too, but never your session",
      "reason": "Step 6.2, condensed 2026 Phase-4 size-reduction. Operator instruction about NOT using the session scratchpad dir for the commit-message file — `--message-file` refuses any path outside repoRoot, a real refusal tested in tests/ship-commit-cli.test.mjs (the escapes-repo case), not a claim /ship's own execution makes about itself beyond that existing binding. Same disposition as the pre-existing 'Not your session scratchpad dir' entry it replaces."
    },
    {
      "line": "scratchpad dir — `--message-file` refuses any path outside `repoRoot`. Why",
      "reason": "Continuation of the line above, naming the refusal + pointing at the new reference file for the full incident (upstream 1c792b2e). Same disposition as the pre-existing entry for this sentence."
    },
    {
      "line": "points at the other, and `--no-tests` always caps the verdict downward",
      "reason": "Step 6.2, condensed 2026 Phase-4. Restates the mutual-exclusivity of `passed`/`converged` and that `--no-tests` only ever downgrades the gate verdict — both already contracted executably/document-only at gate-converged-requires-verified-divergence and gate-no-tests-caps-the-verdict above. This condensed line restates those same claims in shorter form; no new enforcement surface."
    },
    {
      "line": "(`waived` or `not-run`), never up. `passed` is rare **by design** (Steps 2-5",
      "reason": "Continuation of the line above. 'never up' restates gate-no-tests-caps-the-verdict's claim; 'passed is rare by design' is descriptive statistics about the AI-Gate trailer's real-world distribution (measured: 647 not-run, 86 waived, 2 passed), not an enforcement claim. Same disposition as the pre-existing 'passed is rare BY DESIGN' entry it replaces."
    },
    {
      "line": "to chase it. `--no-run-id --gate not-run` disclaims a marker inherited from",
      "reason": "Step 6.2. Names the `--no-run-id --gate not-run` combination and what it disclaims — a usage instruction for the ship-commit CLI's own documented flag, not a /ship exit-code claim beyond what gate-converged-requires-verified-divergence and the sibling document-only gates already cover. Same disposition as the pre-existing '--no-run-id exists' entry it replaces."
    },
    {
      "line": "disposition is `incomplete-expectation` → exit 2. You may omit both flags",
      "reason": "Step 6.3, condensed 2026 Phase-4. Names the `incomplete-expectation` exit-2 refusal on a detached HEAD with no `--expect-detached` — real and mechanically enforced, contracted at guard-b-identity-is-a-precondition (executable, bound above) and pinned by tests/worktree-identity.test.mjs. This condensed line restates that same claim in shorter form; no new enforcement surface."
    },
    {
      "line": "**Continuing work in the same worktree after a squash merge: rebase, never",
      "reason": "Step 6.3, condensed 2026 Phase-4. Git-workflow guidance the agent applies when choosing rebase vs merge after a squash-merged PR — there is no `/ship` CLI exit code to assert, because ship-commit.mjs never sees which git command the agent chose beforehand. Same disposition as the pre-existing 'Continuing work in the same worktree after a squash merge' entry it replaces."
    },
    {
      "line": "| `references/commit-provenance-deep-dive.md` | Step 6.2/6.3's AI-Gate and worktree-identity deep-dive — why each value/flag exists and the incidents behind them. | Debugging `--gate`/`--expect-head`/`--expect-branch` behaviour, or before changing one. |",
      "reason": "The reference-index ROW for the new commit-provenance-deep-dive.md file, added by the Phase-4 size-reduction. Index rows describe what a document covers; they are not gate claims about /ship's behaviour. Matched on 'gate' inside '--gate' and 'AI-Gate', same class as the existing command-example-text entries in this file."
    }
  ]
}
