# Plan: Final Review — Scoped Second Reviewer

This plan narrows the shadow envelope passed to the second reviewer. Files
touched: `gemini-review.mjs`, `model-pricing.mjs`, and `bakeoff-collect.mjs`.

No mention of accepted-debt anywhere in this document — the real
`accepted-debt` transcripts must NOT be paired with this plan; the
`gemini-review`/`model-pricing`/`bakeoff-collect` transcript legitimately is.
