---
name: explain
description: |
  Deliberately over-budget SKILL.md description, used as the poison pill for
  the skills:check description-budget gate. An over-length description is
  silently rejected by Copilot, so the skill stops being selectable without
  anything failing — which is precisely why a gate that reads clean here is
  worthless unless it can be shown to fail.
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  This filler sentence exists solely to push the description past the 1024-character Copilot budget so the gate has something real to reject. 
  Triggers on: "poison fixture only".
---

# Poison fixture

Not a real skill.
