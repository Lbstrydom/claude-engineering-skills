// Fixture: stand-in "tests" file referenced by the lying-skill's gates.
// Content is irrelevant to the oracle checks — its only job is to exist
// and mention each gate id, satisfying the tests[]-reference rule so the
// three divergences below are isolated to oracle behaviour, not this rule.
// always-green, fake-tiered-shadow, fake-convergence
