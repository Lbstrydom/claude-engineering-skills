#!/bin/sh
# Auto-update skills after git pull.
#
# TRACKED — wired via core.hooksPath=.githooks by scripts/install-git-hooks.mjs
# on `npm install`. (The old comment credited "setup.mjs", which does not exist
# — nothing installed this hook; it was untracked per-machine state.)
# To disable locally without editing tracked source: git config core.hooksPath .git/hooks

echo "  [post-merge] Updating skills..."
node scripts/build-manifest.mjs 2>/dev/null
node scripts/install-skills.mjs --local --surface claude --force 2>/dev/null
echo "  [post-merge] Skills updated."
